"""tools.collect — split implementations of the data collector.

Each module owns one source domain; tools/data_collector.py is the facade
that re-exports everything and keeps the DataCollector class API stable.
"""
