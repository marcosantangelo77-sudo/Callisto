# Extracted slices of tools/backtest.py (slice-2 diet).
#
# tools/backtest.py remains the public facade: BacktestEngine re-binds the
# functions in these modules as methods, so call sites and signatures are
# unchanged.
