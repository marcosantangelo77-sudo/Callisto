# speed — run 9, 2026-08-24 evening (perf/standing-speed-0824-190301, worktree edgar)

## Recovery: none needed, first time in nine runs

Branch already contained the full lineage (runs 1–8 all ancestors of
`a6e4467`). No merge, no re-base. Offline baseline re-measured before any
work: wall 0.192s at M=0/F=0 with 6 model calls / 15 fetches / 1445 B;
structure identical to runs 5–8's numbers.

## PROFILING THIS RUN — what the measurements said

**1. Run 8's named candidate (gpu1 ~10s dead failover) is FALSIFIED on this
machine.** Measured tonight: a refused connect to `localhost:8080` costs
17.6ms via `localhost`, 1.6ms via `127.0.0.1`. The in-place transport retry
adds one 0.5s sleep. Total dead-gpu1 tax per routed call ≈ **0.52s**, not
~10s. Not the bottleneck; not touched.

**2. The live path is paying the fork tax on EVERY model call, because run
7's proxy endpoint never activates in production.** Evidence chain:

- `config/providers.yaml` declares `ox_alpha_proxy` with ONLY
  `base_url_env: OX_ALPHA_PROXY_BASE_URL` — no static default.
- `_endpoint_from_config()` marks an env-backed endpoint `_unresolved` when
  the var is unset, and unresolved endpoints are skipped by routing.
- Measured: NO running process on this machine has that var set (`ps eww`
  across dispatcher shells and hermes forks: zero hits).
- Therefore every live routed call resolves candidates to
  `['gpu1', 'ox_alpha']` — measured directly tonight — and lands on a fresh
  `hermes -z` fork. FIVE such forks were visible running concurrently during
  this session; one completed live pipeline run (unemployment question,
  sealed 23:09:48Z) shows "evidence age at seal: oldest 302s" — five minutes
  from first fetch to seal for a three-leaf question.

Measured BEFORE (production shape, ambient shell, trivial completion):

| leg                                   | cost tonight        |
|---------------------------------------|---------------------|
| routed call, production shape          | **11.30s** (fork)   |
| same call through running proxy (:8646)| 4.56–12.83s incl. Portal 429 variance; warm 200s at 1.2–2.4s per runs 7/8 |
| dead-endpoint refuse (either local port)| 1.6–17.6ms         |

The proxies are RUNNING on 8645/8646 right now — idle — while forks burn
11–20s per call. The win exists and is switched off by an env var.

## MODULE CLAIMED: `inference.py::_endpoint_from_config` + `config/providers.yaml`

(plus tests: new `tests/test_speed_proxy_default.py`; updated pin in
`tests/test_speed_persistent_proxy.py` — both files inside this role's owned
set. Nothing else touched.)

Fix, two small pieces:
1. `providers.yaml`: give `ox_alpha_proxy` a static default
   `base_url: http://127.0.0.1:8645/v1`; keep `base_url_env` as the override
   for other machines/ports.
2. `_endpoint_from_config`: env-if-SET takes precedence over the static
   value (today env is consulted only when the static value is absent, so a
   static default would silently kill the override). Affects only entries
   declaring both keys — today exactly one: ox_alpha_proxy.

Resulting behaviour:
- Proxy running (this machine, tonight): decompose + leaf-answer calls go
  over persistent HTTP. Same model behind the same Portal OAuth as the CLI
  serves; only WHERE/WHEN the identical completion is served changes — the
  principle runs 7/8 established.
- Proxy absent: connect-refused in ≤18ms, one 0.5s in-place retry, then
  failover EXACTLY as before; the failed attempt starts the existing
  exponential cooldown (2s→60s cap), so steady-state absence tax ≈ one
  sub-second hit per minute of process life. Degrade-not-crash preserved.

Honest scope limits (not changed):
- Schema-bearing calls (e.g. `agp.adversary` passes `VERDICT_JSON_SCHEMA`)
  still skip the proxy: it declares `structured_output: false` honestly, and
  capability-based selection filters it. The adversary keeps its own call on
  today's path. Changing capability declarations is NOT part of this run.
- gpu1/gpu1_fast/frontier resolution is byte-identical (frontier has no
  static base_url → still _unresolved without its env var).

Correctness plan: pins for precedence (env-set wins), resolved-by-default
(no env), routing includes proxy ahead of CLI without env, dead-default-port
fast failover to CLI, frontier/gpu1 unchanged; adjacent speed suites green;
offline fingerprint byte-identical; five-question Brier golden regression
PASS; then AFTER measurement live and the ratio reported.
