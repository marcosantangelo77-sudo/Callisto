# REVIEW RUN 9 — 2026-08-25 (branch review/ox-alpha-0824e, vs origin/master 6977793)

**Families hunted:** #2 (fix lands in one copy while mirrors keep the bug —
sixth instance), #3 (absence treated as success — S1 got WORSE), #1/#4
(the reviewer defect ledger itself is being silently destroyed by merge
trains). Method: claims-vs-code audit of the four newest merged fix branches
(r1-planner-crash, r2-numeric-gate-landing, r3-verified-empty-leaf,
battery-rerun); live probes against a detached origin/master worktree;
one mutation check.

**Deliverable:** `tests/test_review_2026-08-25_run9.py` — 4 failing repros,
each verified to fail on pristine origin/master for its asserted reason.
(Verified in /tmp/rm9-master worktree; the file also lives on this branch.)

---

## 1. DOES THE CLAIM MATCH THE CODE? (newest merges)

### R1 planner crash (`fix/r1-planner-crash`, 750be5d) — claim TRUE
`query_builder.py:~822` now sorts `-len(p[1])`; the grep-for-siblings claim
checks out (remaining unary-minus keys are numeric). The degradation wrapper
in `retrieval.py:~634` exists ("planner error: …" recorded per source).
Both new suites pass. Honest report.

### R2 numeric gate (`8740be5` cherry-pick + 23a253c) — claim TRUE, and the
stranding diagnosis (fix lived only on unmerged `fix/redteam-backlog-sweep`)
is the best merge-archaeology finding this repo has produced. Gate code
matches the write-up: prefix floor `_prefix_ok`, question-only denominator,
min-two-topical-tokens, D4 window route preserved. Probes confirm wrong-year
and no-year numeric bodies still rejected.

**But see defect C below: the same fix made the zero-result admission WORSE.**

### R3 non-answering leaf (`2c29c29`) — claim TRUE with one large omission
`answers_question` exists exactly as described: declared in model JSON,
read as-is at engine.py:551, one predicate at engine.py:1014, defaults True
for legacy checkpoints (`_leaf_from_payload` passes it through asdict
round-trip — verified). Suite green; my own mutation (neuter
`answers_question` in the predicate) flips its regression test red and
restoring goes green — these tests are load-bearing.

**The omission:** the commit message says "No second gate exists to drift."
False. Two second copies exist and were not touched — run 8's HIGH defect A,
now in its SIXTH instance of family 2:

- `tools/why.py:389` still computes `proposed = max(answered)` ignoring
  BOTH `gap_kind` AND `answers_question`. Live probe: mixed parent sealed
  at 0.40 → why.py reports proposed **0.90**, the confidence of a leaf the
  engine itself ruled as determining nothing. The user-facing explanation
  of a correct seal now contradicts the seal on TWO axes instead of one.
- `tools/calibration/instrument.py:348` unchanged (same double-blindness),
  and still unreachable behind the package ImportError (defect B).

R3's own findings file names the general rule — "every place that selects
or aggregates leaves by structural score must filter through
`answers_question`" — and then does not apply it to the two places that
select leaves by structural score. The once-fix was written down and
skipped.

### battery-rerun — results.jsonl present (40 rows); not independently re-run.

---

## 2. CONFIRMED DEFECTS — ranked

### A · HIGH · family 2 (#6) — best-leaf mirrors blind to gap_kind AND answers_question
Repros: `test_why_explainer_stands_on_nonanswering_leaf`,
`test_why_explainer_still_ignores_gap_kind`. The second test pins run 8's
defect A first half so the eventual fix cannot resolve one axis and leave
the other.

### B · MEDIUM · family 1 — tools.calibration un-importable, DAY FOUR
Filed 08-23 (ed1cc34/R2), re-filed run 8. Three-plus merge trains since;
still `ImportError: cannot import name 'replay_chain'`. Inert module, so
nobody notices. Repro:
`test_calibration_package_imports_after_two_days_red`.

### C · HIGH (upgraded from run 8's MEDIUM) · family 3 — zero-result echo envelope now scores **100%**
S1's CRITICAL filing showed the gate admitting an empty envelope at 75%.
The R2/R2b fix made it perfect: `extract_text` walks the whole payload,
including `meta.query`, which echoes the question verbatim — every
question token matches, coverage = 100%, admitted with the gate's most
confident reason string. Verified live on pristine master:

    RelevanceGate.judge(q, "factual", {"meta": {"query": q, "count": 0},
                                       "results": []})
    -> (True, 1.0, "content covers 100% ...")

Absence is not merely treated as success any more; the emptier the result
set, the more perfectly it echoes the question and the higher it scores.
The structural fix is trivial (judge on results content, never meta; or
refuse envelopes whose parsed text contains no node outside the echoed
query) — but note the shape: the relevance fix and the zero-result bug
live in the SAME function, and the fix's own tests did not include the
empty case. Family 3 says feed every gate an EMPTY input; nobody did.
Repro: `test_relevance_gate_rejects_zero_result_envelope`.

### D · HIGH · process/family 4 — the entire reviewer defect ledger has fallen out of master
Every repro suite from runs 6–8 is MISSING from origin/master:
test_review_2026-08-25.py, test_review_2026_08_23.py,
test_review_2026-08-24d/e.py, test_redteam_source_registry.py,
test_redteam_source_control_plane.py, test_redteam_domain_plugins.py.
Run 8 explicitly restored two of them into its branch and recommended
review-repro files be append-only; the next merge train resolved them away
anyway. Consequence is concrete: defects A/B/C above are UNPINNED on
master right now — no test on master fails for them. The repo's memory of
its own known defects now lives only on side branches that each new train
overwrites. This is the third consecutive run to flag some version of it;
this run quantifies it: ALL of it is gone. Recommendation stands but needs
teeth: merge trains must diff `tests/test_review_*` +
`tests/test_redteam_*` against the union of all open review branches and
treat deletion as a conflict requiring human resolution.

---

## 3. WHAT HOLDS UP

- R3's core mechanism: declared-not-parsed signal, legacy default True,
  checkpoint round-trip, refusal naming. Mutation-tested by me: its suite
  fails when the predicate is neutered, passes restored.
- R1 fix + degradation wrapper, sibling-grep clean.
- R2 prefix floor / denominator direction: junk scoring got STRICTER, not
  looser; D4 route refuses no-year and wrong-year bodies (probed live).
- seal_unprovable suite still green alongside R3's (single-predicate claim
  holds inside engine.run itself).

## 4. IS THE WORK ACTUALLY USED?

- `tools.calibration`: inert behind ImportError (day 4).
- No change to prior inert-component findings (CrossRunMemoryStore,
  plugin registration) — untouched by this train.

## RANKED SUMMARY

| # | Sev | Defect | Repro |
|---|-----|--------|-------|
| C | HIGH | zero-result envelope admits at 100% (worse post-fix) | fails |
| A | HIGH | why.py + instrument.py ignore gap_kind AND answers_question | 2 fail |
| D | HIGH | all reviewer/red-team repro suites absent from master | process |
| B | MED  | tools.calibration un-importable day 4 | fails |

Run:
```
python3 -m pytest tests/test_review_2026-08-25_run9.py -q   # 4 failed (expected)
```
