# SPEED FINDINGS — 2026-08-23 (standing role, run 4 — branch perf/standing-speed-0823-172031)

Worktree: `~/callisto-wt/gate`. Prior runs: `findings/speed_2026-08-23_run3.md`
(merged into this branch).

## THE HEADLINE OF THIS RUN: run 3's work was stranded AGAIN

Run 3 landed parallel-leaves + per-source fan-out + the R1 checkpoint fix on
`perf/standing-speed-0823-164040` (commit `98121d4`) and pushed it. But that
branch is NOT an ancestor of master (`ee549f8`), and this worktree started at
`ee549f8` with ZERO of the speed work present. Measured before touching
anything:

```
python3 scripts/profile_pipeline.py --model-delay 3.0 --fetch-delay 0.8
sealed=True leaves=5 fetches=19 conf=0.55 wall=33.448s   (serial)
```

Worse than run 3's 24.59s serial reading: K1 (`ee549f8`, the calibration-table
fix) added model-call work to the same serial critical path, so every day the
speed fix sits unmerged the baseline gets SLOWER.

## WHAT LANDED

One merge, nothing new written:

    d68e673  Merge commit '98121d4' into perf/standing-speed-0823-172031

Clean merge; no conflicts. Brings in:
- parallel leaf engine (engine.py restructure, Phase A fetch / Phase B answer)
- parallel per-source fan-out inside retrieval rounds (retrieval.py)
- R1 checkpoint fix (per-leaf saves inside the concurrent worker, atomic)
- golden suite + Brier five-question regression + checkpoint-partial-progress test
- scripts/profile_pipeline.py + scripts/gen_speed_golden.py

## MEASURED RATIO (same instrumented profile, M=3s/F=0.8s)

| tree                        | wall     | busy_s | model calls | bytes |
|-----------------------------|---------:|-------:|------------:|------:|
| pre-merge (serial)          | 33.448s  | 33.27  | 6           | 2590  |
| post-merge                  | **7.744s** | 33.31 | 6           | **2590** |

**4.32x end-to-end on this branch's own before-state** (run 3 reported 3.2x on
its pre-K1 baseline). Busy time identical (33.27 vs 33.31s) and bytes fetched
identical (2590) — pure re-parallelisation, no work skipped.

## CORRECTNESS

- `tests/test_speed_parallel_leaves.py` +
  `test_speed_parallel_sources.py` +
  `test_review_checkpoint_partial_progress.py`: **20/20 pass**, including
  fingerprint equality fresh-vs-resumed.
- Five-question Brier regression vs serial golden: **pass** (golden carries
  the documented declared-stance regeneration rule from run 3).
- Adjacent suites w1-retrieval / p1-retro / p1-pipeline / retro-hardening /
  w3-checkpoint / i4-retro-batch: **74/74 pass.**
- No caching introduced; the adversary remains its own separate model call;
  nothing here can raise confidence.

## PROCESS NOTE — the stranding pattern is now the dominant cost

Three runs in a row, the previous run's verified speedup was not where the
next run started. Merges are a human decision per COORDINATION.md, but a
standing speed role that cannot land on master measures 4x and leaves the
system at 1x. Recommendation to the merge pass: land `98121d4`'s lineage
(now also at `d68e673` on `-172031`) ahead of any further optimisation.

## NEXT BOTTLENECK (unchanged from run 3, now confirmed twice)

At M=3s/F=0.8s the critical path is decompose(3s) + max-leaf(answer+fetch)
+ adversary(M). Retrieval overlap is done. The remaining structural costs
are model-call count and prompt size, or real transport latency once the
persistent worker pool (`perf/persistent-transport-v2`, b39aa77) lands —
re-price with live F before doing prompt work.
