# speed — run 16, 2026-08-24 late night (perf/standing-speed-0824-225243, worktree loop)

## Recovery

Worktree branch had ZERO of the speed lineage again (sixth consecutive run).
Merged `origin/perf/standing-speed-0824-222421` (runs 1–15 incl. run 15's
429-patience policy: attempts 5, budget 120s, `_RateLimitExhausted` flat
cooldown). 53/53 speed tests green on the merged tree before any new work.

## PROFILE FIRST

Offline structural (`scripts/profile_pipeline.py --model-delay 3.0
--fetch-delay 0.8 --leaves 5`): wall 6.995s · busy 30.077s · 6 model calls ·
15 fetches · 1445 B — matches runs 5–15 structure; nothing regressed.
With ALL delays zeroed (M=0/F=0): wall 0.145s — pure compute/serialization/
I/O overhead is negligible; there is no hidden local cost left in the
pipeline body itself.

Live tonight (two proxies up on 8645/8646, GPUs down, no frontier env):

| leg                                   | measured              |
|---------------------------------------|-----------------------|
| raw curl → proxy, one-word reply      | 1.2–2.6s, **HTTP 429 body** (Portal capacity) |
| routed research_synthesis ×4          | 9.5s / 76.2s / 73.1s / 102.6s — all served by ox_alpha_proxy; run-15 patience holding calls in place, zero CLI forks |
| dead gpu1 hop                          | ms (run 12 connect-phase fix) |

The serial critical path of a live question is decompose(M) + fetch(F) +
best-leaf answers(≈max of N parallel M calls) + adversary(M) = 3 model-call
rounds. Query refinement is deterministic (no model call); each leaf answer
is exactly one call (+1 only when the model requests sandbox compute).
Call count is already minimal under the hard rules (the adversary stays its
own call). Portal upstream capacity dominates each round tonight and is
external.

## THE FINDING — the critic is banned from the fast tier

`agp/adversary.py:479` calls `complete(task_class, messages,
schema=VERDICT_JSON_SCHEMA)` — the adversary is the pipeline's ONLY
schema-bearing routed call. `ProviderRouter.candidates_for()` excludes any
endpoint with `structured_output: false` from schema-bearing calls UNLESS
its backend is `hermes_cli`. Measured on this machine:

    candidates_for("adversarial_review")             -> ['gpu1', 'ox_alpha_proxy', 'ox_alpha']
    candidates_for("adversarial_review", schema=...) -> ['gpu1', 'ox_alpha']

`ox_alpha_proxy` is openai_compat with an HONEST `structured_output: false`
declaration (upstream is JSON-in-text), so the critic can never ride the
proxy that serves the author. It always falls through to `ox_alpha` — the
fresh-fork CLI path (~12–14s process startup BEFORE its own Portal queue),
the exact cost the proxy exists to avoid — on one of the three serial
rounds of every sealed question.

The hermes_cli exemption exists precisely because best-effort JSON-in-text
plus the shared tolerant parser keeps a laptop running the whole system
(inference.py:1306–1313). The proxy serves THE SAME MODEL behind the SAME
Portal OAuth (parity established run 7, re-verified runs 9/11); excluding it
while exempting the CLI is an accident of backend naming, not a policy.
Battery D3's parse-retry/fail-closed machinery applies identically over
either transport.

Also verified live tonight: Portal ACCEPTS `response_format: json_schema`
through the proxy without erroring (200, valid JSON back) — so admission is
safe — while `_payload()` currently attaches response_format even to
endpoints declared unable to enforce it, which is dishonest about what the
request can achieve and would hard-400 on stricter OpenAI-compat providers.

## MODULE CLAIMED THIS RUN: `inference.py`

(capability-admission filter ONLY: `candidates_for`, the fallback list
inside it, `_payload`'s schema attachment, `pick_endpoint` alignment; plus
new tests/test_speed_adversary_transport.py, scripts/live_price_adversary.py,
this findings file. No other file touched.)

Fix:
1. `candidates_for`: extend the best-effort exemption from
   `backend == "hermes_cli"` to also cover `openai_compat` endpoints that
   honestly declare `structured_output: false`. Policy unchanged for every
   other backend; endpoints declaring true are unaffected; unresolved-env
   endpoints stay skipped.
2. `_payload`: attach `response_format` only when the endpoint declares
   `structured_output: true` — matching hermes_cli's honest "no enforcement,
   tolerant parse downstream" semantics byte-for-byte at the request level.
   No current caller's bytes change (false-declared endpoints were
   unreachable for schema calls before this fix).
3. Mirror both rules in the all-cooling-down fallback list and in
   `pick_endpoint`'s filter so the three filters cannot drift apart again.

Correctness analysis: which MODEL serves the adversary is unchanged — proxy
and CLI are the same stealth/ox-alpha behind the same Portal OAuth. The
adversary remains its own separate call (asymmetry guarantee untouched);
no caching, nothing crosses any retrodiction cutoff, no gate or threshold
moves. Offline harness never enters the router → goldens byte-identical by
construction. The five-question Brier golden must not move.

Measurement plan: paired live A/B — the identical schema-bearing request via
(a) the as-is routed path (lands on CLI fork) vs (b) production
`_post_with_retry(_post, ...)` against the resolved ox_alpha_proxy endpoint;
interleaved A/B/A/B against tonight's capacity drift. Offline fingerprint +
all speed suites + Brier golden regression as gates. Pins: admission order,
hermes_cli still admitted, true-declared unaffected ordering, payload omits
response_format iff declared false, fallback consistency, integration
through complete() reaching a live loopback stub as the second candidate
behind a dead first.

Falsifier: if Portal begins REJECTING schema-bearing bodies via the proxy,
or the proxy's adversary verdicts diverge from the CLI's on the same prompt
beyond transport noise, revert — the critic's independence and fail-closed
behaviour outrank any latency win.

## SHIPPED

- `inference.py::candidates_for` + its fallback list: best-effort admission
  of false-declared openai_compat endpoints for schema-bearing calls
  (policy boundary pinned: llama_cpp_server stays excluded — that
  declaration means the server lacks its grammar constraint).
- `inference.py::_payload`: response_format attached ONLY to true-declared
  endpoints; `getattr(..., True)` keeps legacy TierConfig shapes at
  pre-run-16 bytes. Best-effort endpoints get exactly the CLI's contract:
  no enforcement block, tolerant parse downstream, battery-D3 fail-closed
  either way.
- `inference.py::pick_endpoint`: aligned with candidates_for (it was
  stricter than even the old rule — it skipped hermes_cli too).
- RESTORED run 12's connect-phase fast-failover into `_post_with_retry`.
  The runs-14 recovery merge had silently dropped it while its commit
  message claimed it kept run-12 semantics; the tier5 pool suite caught
  it (dead hop took 5 in-place attempts + backoff after run 15 raised
  attempts to 5). Pin updated to the correct post-run-12 contract:
  ONE attempt on ConnectError/ConnectTimeout, immediate failover.
- tests/test_speed_adversary_transport.py: 11 pins (admission order,
  CLI still admitted, true-declared priority, unresolved-env still
  skipped, llama exclusion boundary, payload honesty both ways,
  fallback/pick_endpoint consistency, end-to-end schema-bearing call
  reaching a best-effort secondary with per-hop payload proof).
- Reversed pre-run-16 pin in tests/test_speed_proxy_default.py documented,
  not deleted.

## CORRECTNESS GATES

- Speed suites + tier5 router/pool + oxa provider suites: 132 passed.
- Offline fingerprint: 6 model calls / 15 fetches / 1445 B, wall 6.945s
  (band 6.93–7.00 across today's runs) — unchanged.
- Five-question retro_batch Brier golden regression: PASS at 0.25.
- The adversary remains its own separate call on every path; which MODEL
  serves is unchanged (same stealth/ox-alpha behind the same Portal
  OAuth); no caching; nothing crosses any cutoff; no gate moved.

## MEASURED AFTER — live, paired, interleaved (tonight)

Six paired legs (scripts/live_price_adversary.py), same messages, same
VERDICT_JSON_SCHEMA:

| leg   | n | mean   | min    | max     | ok |
|-------|---|--------|--------|---------|----|
| PROXY | 6 | 44.7s  | 6.8s   | 133.1s  | 6/6|
| FORK  | 6 | 16.8s  | 11.8s  | 26.3s   | 6/6|

HONEST READING, two facts in tension:

1. STRUCTURAL WIN (floors, stable across nights): the fork leg NEVER beat
   ~11.8s — that is fresh-interpreter startup, paid before any upstream
   wait, by construction. The proxy leg served warm calls in 6.8–9.6s
   under the same conditions, matching runs 7/9/11's 1.2–4.6s warm
   measurements. On a normal-capacity night the critic now rides the fast
   tier instead of always paying ≥12s tax on one of three serial rounds.
2. TONIGHT'S INVERSION (means, saturated night): Portal capacity windows
   dominated the proxy leg (40–133s waits) while fork legs were served in
   ~13s. Under THIS regime the pre-run-16 path would have been faster for
   the adversary, because run 15's 120s patience holds proxy calls in
   place while the CLI's shorter queue sometimes wins.

The inversion implicates run 15's dominance assumption ("both tiers queue
on the SAME upstream, so in-place waiting strictly dominates") — tonight's
paired data REFUTES strict dominance: the two transports do NOT see the
same queue. That finding belongs to the patience-policy owner (run 15's
module claim); I deliberately did not touch `_429_PATIENCE_S` this run.
Falsifier for keeping MY change: a night where admission makes END-TO-END
question walls worse than always-fork under BOTH calm and saturated
capacity. Calm-regime data says the opposite; watch for divergence when
empirical routing (W2) has enough per-(role,transport) measurements to
decide this from records instead of one night's weather.

## NEXT BOTTLENECK

1. Run 15's patience policy needs re-pricing against tonight's evidence:
   if proxy and CLI queues genuinely diverge under load, patience-then-
   fork-on-exhaustion is not always optimal; a bounded EARLY probe of the
   second transport (e.g. fail over after ~30s of stacked windows rather
   than 120s) may dominate both extremes. Needs its own measured run with
   per-leg window instrumentation (distinguish 429-patience sleeps from
   slow TTFT — tonight's walls cannot).
2. Persisted router health/cooldown state ACROSS questions/runs (run 13
   lead #2, still open): every process rebuilds state, re-probing dead
   hops and forgetting cooldowns. Small per call post-run-12, but it is
   per question × per call and trivially cacheable off-cutoff (endpoint
   health is not evidence).
3. Portal capacity variance remains the dominant live wall when degraded
   (external; only mitigable by reducing serial round trips, which the
   hard rules forbid below three).
