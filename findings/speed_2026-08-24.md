# Speed run — 2026-08-24 (branch perf/standing-speed-0824-002737)

## ANNOUNCEMENT (before touching code) — module this run: inference.py

Profile-first conclusion: this branch carries NONE of the six stranded speed
branches (see ../findings/speed_branch_triage.md; tip
origin/perf/standing-speed-0823-225728 contains runs 1–13, none landed here).
Verified on HEAD (1b36c91):

- inference.py `_post()` and `check_health()` build a FRESH
  `httpx.AsyncClient` per request (lines ~1384, ~1316) → TCP+TLS handshake
  every model call. Upstream run6 measured this fix at 1.81x steady-state
  (0.15s → 0.083s/call), answers unchanged.
- inference.py `_post_with_retry` sleeps 0.5s·(i+1) even on connect-phase
  failures (ConnectError/ConnectTimeout) where retrying immediately on a
  different attempt cannot succeed faster — upstream run12 measured dead-hop
  probe 0.528s → <0.05s design target (0.119–0.255s full path).

Engine.py parallel leaves and retrieval.py fan-out are LARGER costs but are
multi-module restructures colliding with this branch's crossrun/checkpoint
work — they need their own run(s), not a ride-along. This run lands ONLY the
inference.py transport pair, ported from the already-measured branch
(225728: `_shared_client`, `_reset_shared_client`, connect-phase failover,
tests test_speed_shared_http_pool.py + test_speed_connect_failover.py).

Correctness gate: pooled client must keep failover/retry semantics identical;
per-request timeout overrides preserved; no caching anywhere near a
retrodiction cutoff (transport holds connections, never responses); adversary
stays its own call (untouched). Verify with the ported pins plus existing
router suites; then re-measure paired before/after against a local TLS-less
endpoint and record the ratio below.

## VERIFIED findings logged this session (no fix attempted — outside my files)

### [VERIFIED] tools/pipeline/engine.py (~line 771 seal path) — AGPSession.artifact_refs / add_artifacts() has ZERO production callers
Blast radius: SILENT
Evidence: repo-wide grep: `add_artifacts` defined agp/__init__.py:287, never
called anywhere; engine extends `self.artifact_refs` (PipelineResult-side)
and passes verify_artifact_gate() pre-seal, but never attaches refs to the
AGPSession it seals, so `session.to_dict()["artifact_refs"]` is always []
inside the keyed-HMAC seal payload. A20's "seal structurally cannot omit the
artifact layer" holds only because the field is empty, not because artifacts
are covered. Review branch origin/review/ox-alpha-0823 (commit 7e75b76)
wired this via session.artifact_check; that commit is NOT an ancestor of HEAD.
Falsifier: a sealed result whose session dict carries non-empty artifact_refs.
For: owner of agp/__init__.py + engine seal path (not the speed role).

### [VERIFIED] branch topology — all measured speed work (runs 1–13) absent from batch3/this branch
Blast radius: LOUD (43-min-class defects remain live on the integration line)
Evidence: git merge-base --is-ancestor origin/perf/standing-speed-0823-225728
HEAD → false; fresh AsyncClient per call confirmed in working tree.
Falsifier: 225728 (or its cherry-picked descendant) landing on master/batch3.
For: merge authority (human decision per COORDINATION.md); speed role ports
piecewise in the meantime.
