# SPEED run 19 — 2026-08-25 · branch perf/standing-speed-0825-run19 (worktree money)

Recovery: worktree money was reassigned to improve/rotating-0825-013056 (clean).
Opened perf/standing-speed-0825-run19 at run-18 tip 89c7c7e (origin verified).

## PROFILE FIRST (structural)

Offline structural run reproduces runs 5–18 exactly (sealed=True, 6 model
calls, 1445 B): nothing regressed upstream of the batch seam. Live transport
was re-priced in run 18: warm proxy 4.07/5.53s median per model call.

The dominant remaining STRUCTURAL cost is run-18's handoff #2:
scripts/run_retro_batch.py -> RetrodictionBatch.run() executes the five
questions in a strict serial for-loop. At ~6 model calls/question and
~4-5s/call warm, one batch pays ~5 x 6 x 4.5s ≈ 135s of pure serial wall;
questions are independent (separate prompts, separate checkpoints, append-only
JSONL keyed by question_id).

## MODULE CLAIMED THIS RUN: tools/retrodiction/batch.py ONLY

Change: bounded concurrency across questions inside RetrodictionBatch.run().
Contract:
  - new BatchConfig.max_concurrency (default 1 = today's exact serial
    behaviour; byte-identical results and JSONL ordering at 1)
  - asyncio.Semaphore-bounded gather when >1; checkpoint hit / run / save /
    append logic UNCHANGED per question; appends stay synchronous blocks on
    the event loop thread (no interleaving possible without awaits)
  - resume/error-row semantics untouched: errors are rows, not dead batches
  - NO caching, nothing crosses a retrodiction cutoff, adversary stays its own
    model call (concurrency shares transport, never context)
Falsifier: a batch whose wall time does NOT drop ~min(N, concurrency)x under
a stubbed-latency researcher, or any Brier delta on data/retro_batch/.

## MEASURED BEFORE/AFTER (scripts/measure_batch_concurrency.py,
--latency 4.5s per question = tonight's warm-proxy per-question model time;
5 scored questions)

| mode | wall | model calls |
|------|------|-------------|
| serial (mc=1, old default) | 22.53s | 5 |
| concurrent mc=3 | 9.02s | 5 |
| concurrent mc=5 | 4.51s | 5 |

Speedup 2.50x at mc=3, 4.99x at mc=5; answers identical by assertion.
Structural profile after landing: byte-identical to runs 5-18 (6 calls,
1445 B). Speed/tier5 + adjacent suites: 119 passed.
Live projection: a 5-question batch at ~6 calls/question and ~4.5s/call warm
pays ~135s serial; at mc=3 the wall approaches ~45s + fetch overlap.

Default stays max_concurrency=1: run_retro_batch.py is NOT flipped this run —
the concurrency-safe claim for FileCheckpointer writes under load needs its
own measured live run before the scored path uses it. The knob exists, is
pinned, and degrades to exactly today's behaviour.

## CORRECTNESS VERIFICATION

- tests/test_speed_batch_concurrency.py (4 pins): serial order preserved,
  answers identical under mc=4, semaphore bound respected (peak in-flight <=
  bound), errors stay rows under concurrency.
- Full speed sweep + i4 retro batch + hardening + r6: 119 passed.
- five-question Brier golden (tests/fixtures/speed_golden/
  five_question_brier.json): PASS at 0.25 — predictions unchanged.
- No caching introduced; nothing crosses a retrodiction cutoff; adversary
  remains its own call (concurrency shares transport objects only).

## NEXT BOTTLENECKS

1. Flip run_retro_batch.py default to max_concurrency=3 AFTER a live
   checkpoint-safety run under load (unverified).
2. Portal capacity windows — external wall, unchanged.
3. Answer-stage prompt size — re-price on a low-noise night.
