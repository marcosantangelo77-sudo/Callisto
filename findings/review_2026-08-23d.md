# REVIEW 2026-08-23d — standing review role, run 4

Reviewer: standing review agent (branch `review/rotating-0823-160255`,
base = master @ 5e88b05). **No production code was edited.** Defects are
reproductions in `tests/test_review_2026-08-23d.py`; controls prove each
repro fails for the stated reason.

Prior runs consulted: run 3 (`review_2026-08-23c.md`, on
`review/rotating-0823-155500`) covered W5, C1–C4, the floor_conf family,
F5/F6 and the merge-pipeline problem. This run targets what run 3 did not:

| Work | Where | What was checked |
|---|---|---|
| Resume differential red-team pass | `fix/w3-checkpoint` @ 0d05fcc (train wt) | do the claimed D1–D3 repros exist, run, and fail for the stated reason |
| Hypothesis-lifecycle pass | epistemics @ e4edcca, 6f4f760, e5a6ae1 | claim vs code; tests vs real migrated schema |
| Wiki clamp + persistence fix | epistemics @ 4352ad1 | half-landed check (mandate item 3) |
| claims.py round() sites | master | re-verify run-3's R5 against the actual arithmetic |
| Source-diversity work | money @ improve/source-diversity | reviewable? (no — see note) |

## What holds up

1. **The resume differential pass (0d05fcc) is honest work.** Its three
   CRITICAL/HIGH repro tests exist (`tests/test_redteam_resume_differential.py`),
   run, and fail exactly as claimed: 3 failed / 6 passed. I reproduced D1
   independently: tamper a saved checkpoint's body, recompute
   `content_sha256` on disk, reload — `sig` is absent (no key configured),
   `replay_ledger` replays the fabricated bytes with `primary=True` and
   reports zero integrity failures, and `provenance_is_intact` returns
   True. `verify_signature` has **zero callers outside checkpoint.py**
   (grep over tools/ + agp/) — save() signs, nothing verifies. D2 is also
   still structurally open on master: `_is_fetch_stage` greps the
   attacker-controlled stage string (`checkpoint.py:441`), so renaming a
   fetch-bearing stage to "decompose" evades the C3 mandatory-structure
   check. These are correct, well-labelled OPEN defects — the best-shaped
   artifact any instance has produced. They must not be lost to an
   unmerged branch.
2. **The hypothesis-lifecycle pass does what it says.** e4edcca's headline
   break is credible and load-bearing: api.py:862 applies pending
   migrations unconditionally at every startup, migration 013 drops
   `hypotheses.sport`, and the writer branched on neither. The new tests
   (`test_build_lifecycle_dualshape.py`) build BOTH shapes through the
   REAL production sequence (`ensure_schema` → `apply_pending_migrations`),
   not fixtures — directly fixing the fixture-shaped-test failure mode.
   All targeted subsets green: dualshape 7/7, resolver/schema-split/
   generic-prediction 48/48, hypothesis 21/21, memory-wiki 8/8.
3. **4352ad1 closes a genuine half-landed fix.** The floor_conf rule had
   been enforced across agp but `clamp_to_ceiling` in
   tools/memory_epistemics.py kept `round()` — one more copy of the same
   bug surviving because the fix was applied per-site rather than via one
   shared quantiser. Fixed downward-only with a 2000-case property sweep;
   and record_learning now persists source_class/seal instead of deciding
   them and throwing them away. Verified the shipped tests pass.

## What does not — ranked

### P0 — the review pipeline is still broken, and now the best new defect record is stranded too

Zero `review/*` or red-team branches are merged to origin/master
(`git branch -r --merged origin/master` lists none of the ten review
branches). Run 3 named this P0; it remains true, and this run adds a
sharper instance: the resume differential pass (D1–D3, the strongest
finding set produced today) lives ONLY on `fix/w3-checkpoint`. An operator
reading master sees checkpoint HMAC verification as solved territory
(C1–C4 merged) when in fact the signature is minted and never checked
anywhere. STRANDED_WORK.md's correction ("no work was lost") is true of
git objects and false of attention: work that exists only on a branch
nobody merges has been lost from every decision that matters.

### P1 — three shipped laundering repros crash instead of asserting: the open-defect ledger is partly fictional

`tests/test_redteam_confidence_laundering.py` (merged, on master) uses
`inherited_ceiling(...)` inside TestInheritanceRule (lines ~260, ~300)
but imports only `clamp_parent_confidence` from tools.research_program.
Result: 3 of the 7 tests in the file die with `NameError:
name 'inherited_ceiling' is not defined` BEFORE reaching their
assertions — including the F7 stale-resolution repro claiming
"lift to 0.7095". These tests were presented as precise reproductions;
two of them have never executed their central claim. The remaining 4
failures are real (verified individually, e.g. mixed-provenance synthesis
scores 1.0 > SECONDARY ceiling 0.75), but a defect ledger whose entries
crash is worse than an empty one: it looks maintained while reporting
nothing. Repro: `test_laundering_repros_execute` in my test file pins the
NameError; fix is a one-line import — owned by whoever maintains that file.

### P2 — run-3's R5 needs correcting, not just re-recording

Run 3 flagged `agp/claims.py:141,145` (`recompute_confidence`) as rounding
UP past a tier boundary. Re-derived: the value is clamped to the ceiling
table FIRST, then rounded, and every ceiling in MAX_CONFIDENCE_BY_SOURCE is
already 2dp — so rounding can land ON the ceiling (0.7499→0.75) but never
PAST it. The violation is of the letter of "a clamp may only move down"
(+0.0001 within-ceiling), not of the tier system itself. Downgrade R5 from
defect to style-consistency item (use floor_conf for uniformity); do not
let it distract from F5/F6, which remain the real open holes.

### R10 (new, minor) — signed-age regime shares W5/C4's operability gap, now with a number

harness_key() resolves CALLISTO_CUTOFF_KEY / CALLISTO_SEAL_KEY; neither is
set by any launcher script in the repo, and all checkpoints in the live
state dir carry no sig. trusted_age_seconds() therefore falls back to the
raw produced_at claim everywhere outside pytest. Same shape run 3 called
P3; reconfirmed after 0d05fcc, whose D1 depends on the same env keys.

### Note — money/improve/source-diversity is not reviewable yet

Its latest commit is an autosave snapshot mid-flight (independence-family
fan-out budgeting in retrieval.py, uncommitted intent). Nothing claimed;
nothing to check claims against. Next run should pick it up once the
instance commits with a message.

## Inert / unwired (mandate item 5, delta vs run 3)

- No change: AdversaryPanel still has no production caller;
  checkpointing still wired only in scripts/run_retro_batch.py;
  ProvenanceLedger durability unchanged. One addition: `verify_signature`
  joins the inert list ON MASTER (its only would-be consumers arrive with
  the stranded 0d05fcc fixes).

## Verdict

Today's newest work is better than yesterday's: the differential pass and
the lifecycle pass both test against reality (real tampered files, real
migrated schemas) instead of fixtures, and their claims survive checking.
But the repo's two systemic failures persist untouched — findings and
fixes pile up on branches that never merge, and a merged "open defect"
file contains repros that crash before asserting. Merge `fix/w3-checkpoint`
and repair the laundering test imports before anyone writes another line
of capability.
