# speed — run 8, 2026-08-23 late night (perf/standing-speed-0823-231635, worktree gate)

## Recovery

Worktree branch was `review/rotating-0823-191215` (reassigned again). Cut fresh
branch `perf/standing-speed-0823-231635`, merged
`origin/perf/standing-speed-0823-182830` (runs 1–7 incl. persistent-proxy).
16 speed tests pass on merged tree. Offline baseline re-measured:
wall 7.735s · busy 33.34s · 6 model calls · 19 fetches · 2590 B — identical
to run 7. Nothing regressed.

## LIVE RE-PRICING (per runs 5/6 standing instruction)

Proxy running (`hermes proxy start --provider nous --port 8646`). Raw curl,
one-word reply: warm 200s at **1.7–2.4s**, BUT **4 of 9 calls tonight returned
429** (Portal capacity) in 0.5–2.4s.

Through ProviderRouter with OX_ALPHA_PROXY_BASE_URL set:

| call                          | result                                        | time   |
|-------------------------------|-----------------------------------------------|--------|
| research_synthesis            | proxy 429 → gpu1 conn-refused → **CLI fork**  | 14.34s |
| adversarial_review            | proxy 429 → gpu1 conn-refused → **CLI fork**  | 14.80s |

So under tonight's capacity pressure the run-7 proxy win is thrown away on
the very first 429: `_post_with_retry` retries only >=500/transport errors;
a 429 (<500) raises immediately, endpoint records failure + enters 2s+ cooldown,
and the call lands on the ~12–20s fresh-fork path anyway.

## MODULE CLAIMED THIS RUN: `inference.py` — `_post_with_retry` 429 policy ONLY

(plus new tests/test_speed_429_retry.py; no other file touched)

Fix: treat upstream 429 as retryable-in-place, like 5xx already is:
- 429 no longer raises on first attempt; bounded extra attempts with backoff
  honouring Retry-After (capped) when present.
- Non-429 4xx still fail over immediately (unchanged).
- Exhaustion still propagates → existing failover chain unchanged.
- Same model serves the retry (proxy == same ox_alpha behind Portal), so no
  answer-affecting routing change: only WHERE/WHEN the identical completion is
  served. No caching, nothing crosses a cutoff, the adversary stays its own
  call, no gate moves.

Correctness plan: unit pins for all four behaviours; adjacent speed suites +
Brier golden regression must stay green.

## SHIPPED — commit 7f0b41c (branch perf/standing-speed-0823-231635, pushed)

`inference.py::_post_with_retry`: 429 now retries in place (bounded, 2
attempts like 5xx), honouring Retry-After delta-seconds capped at 10s; a
Retry-After larger than the cap declines the retry and fails over (a server
demanding 30s of backoff must not stall a caller whose failover costs 14s).
Non-429 4xx semantics unchanged; exhaustion still propagates to the existing
failover chain. New `_retry_after_seconds()` helper + two constants.

Tests: tests/test_speed_429_retry.py, 6 pins (retry-then-success, non-429
immediate raise, exhaustion propagates, Retry-After cap/default/garbage,
5xx timing byte-compat, Retry-After:0 immediate retry). Adjacent suites:
49 passed total incl. tier5 router suite. Brier five-question golden: PASS.
Offline wall re-measured post-change: 7.707s (baseline 7.735s, within noise;
simulated delays dominate — this fix targets LIVE latency).

## MEASURED AFTER (live)

Portal tonight sends `Retry-After: 30` on its capacity 429s — so under THAT
regime the cap correctly fails over and behaviour matches pre-run8 (no
regression). The fix wins when a 429 is transient without a long backoff
demand: measured live call through ProviderRouter on proxy tier:
- before fix: any 429 -> instant failover to fresh-fork CLI, 14.3-14.8s/call
- after fix, transient 429 retried: 5.1s/call (vs 14.3s) = ~2.8x on that call
Raw-curl control confirms ~50% of Portal calls 429'd tonight with occasional
200s at 1.7-2.4s — capacity is genuinely intermittent, not hard-exhausted.

Honest caveat: when Retry-After >= cap, run-7's numbers do not return; that
is Portal capacity, not transport. Falsifier for the whole finding: a night
where 429s always carry large Retry-After AND the CLI fork is faster than
waiting — then this path is dead code, harmless by construction.

## NEXT BOTTLENECK

Unchanged from run 7's analysis: answer-call prompt size (4,000-char evidence
bodies embedded per item), to be priced live when Portal capacity allows a
clean A/B. Also noted for the router owner: `gpu1` sits AHEAD of ox_alpha_proxy
in every task_class list and conn-refuses in ~10s per attempt before the proxy
is even tried — on this machine (no GPU serving) that is ~10s of pure dead
failover per call, likely bigger than anything else left. Not touched this run
(ordering change alters routing semantics; needs its own measured run).
