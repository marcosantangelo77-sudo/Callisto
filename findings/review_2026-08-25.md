# REVIEW RUN 8 — 2026-08-25 (branch review/ox-alpha-0824e @ origin/master a6e4467)

**Families hunted:** #1 (verification that never runs — production-inert
mechanisms), #2 (fix lands in one copy while another keeps the bug), #3
(absence treated as success). Method: re-ran every prior reviewer's canary
suite against the newest sm1 master; claims-vs-code audit of the three newest
unreviewed agent reports; then grep-and-probe for OTHER copies of every rule
touched by the newest merged fix (seal-unprovable).

**Deliverable:** `tests/test_review_2026-08-25.py` — 4 failing repros on
current master, each verified to fail for its asserted reason. Also restored
two prior reviewers' suites that had been dropped from the tree (see §3).

---

## 1. DOES THE CLAIM MATCH THE CODE? (newest unreviewed reports)

### fix/seal-unprovable (merged in a6e4467) — claim TRUE, fix real
`findings/seal_unprovable.md` claims: parent stands only on provable leaves;
all-gapped → refuse naming the kinds; mixed → seal capped at SELF_REVIEW_CEILING
standing on provable legs only. Verified in code (engine.py:972-1017) and by
running its suite: `tests/test_seal_unprovable.py` **5/5 pass**. The fixture
updates it declares are present. This is an honest, complete report.

### redteam/source-control-plane @ c7f93d4 — claims TRUE
Re-ran `tests/test_redteam_source_control_plane.py`: **5 failed / 3 passed**,
exactly as the report states. Independently reproduced H1's core fact myself:

```
registered but UNPROBED : cftc_cot, cmefedfut, sec_fulltext, semanticscholar
probe keys w/o a source : cftc, sec_fts, semantic_scholar
```

### redteam/source-registry @ c4b5942 — claims TRUE
Re-ran `tests/test_redteam_source_registry.py`: **13 failed / 5 passed**, as
claimed. Spot-reproduced S1's unit root live: `RelevanceGate.judge` admits
`{"meta": {"query": <the question>, "count": 0}, "results": []}` at **75%
coverage** — absence IS the success, on master, today.

### redteam/domain-plugins @ 8d287cd/187c674 — claims TRUE
Restored `tests/test_redteam_domain_plugins.py`: **7/7 fail before fix** as
stated. Verified R3's wiring-audit claim directly: `_default_registry()`
(orchestrator.py) registers only sports + compute; finance/kalshi/
source_registry plugins have zero production `register_if_available` callers.

## 2. CONFIRMED NEW DEFECTS — ranked

### A · HIGH · family 2 (fifth recorded instance) — the seal contract half-landed: its two mirrors still stand on gapped leaves
The seal-unprovable fix taught engine.run one rule: *a leaf with gap_kind
proves nothing*. But the rule now exists in exactly ONE of THREE copies:

- `tools/why.py:389-390` — `explain_result`, the USER-FACING explanation of
  the seal, picks `max(answered)` ignoring `gap_kind`. On a mixed run it
  reports the parent's proposal as **0.55 — the confidence of an unprovable
  leaf** — while explaining a sealed score of 0.40. The explanation
  contradicts the thing it explains.
- `tools/calibration/instrument.py:348` — `run()` feeds
  `replay_parent_chain(best_leaf=max(answered))` including gapped leaves. The
  instrument's whole purpose is "replay reproduces observed final exactly";
  fed a real mixed run's leaves it replays 0.55 vs observed 0.40 and reports
  `verified=False`, i.e. it misattributes a CORRECT seal as unexplained drift
  — or would bless a regressed engine that matched it.

Same shape as the original forecast-sign and floor_conf bugs: fixed at the
call site that was caught, mirrored copies untreated.
Repros: `test_why_explainer_stands_on_gap_classified_leaf`,
`test_calibration_parent_replay_uses_gapped_best_leaf`.

### B · MEDIUM · family 1 — tools.calibration STILL un-importable; third day red, second reviewer to file it
ed1cc34 (2026-08-23) filed R2 "calibration package broken" with repros. Since
then three merge trains ran. Nothing changed: `import tools.calibration`
raises ImportError (`replay_chain` does not exist in instrument.py;
`tools/calibration/bridge.py` has NEVER existed in any commit reachable from
master — the import has been dangling since the package was born). The
underconfidence-measurement component — the thing that would tell us whether
sealed confidence means anything — cannot be imported by anything but its own
never-run tests. This is also the answer to "is the work used": no, nothing
imports it; the ImportError is invisible because the module is inert.
Repro: `test_calibration_package_imports_after_two_days_red`.
(Its instrument internals DO work when loaded directly — which is how defect
A's second half was demonstrated.)

### C · MEDIUM · family 3 — zero-result metadata echo admission still live after CRITICAL filing
S1 (redteam_source_registry.md) is marked CRITICAL with end-to-end repros
(two empty hosts ⇒ "sufficient: 2 independent sources"). Filed 23:09 last
night; not fixed on the newest master. Pinned as a control so the fix flips
loudly: `test_relevance_gate_rejects_zero_result_envelope_on_master`.
Not a new discovery — this entry records that a CRITICAL red-team finding
with committed failing tests survived a merge train untouched, which is the
report→fix→merge tracking gap run 6 already flagged, now with a concrete
cost example.

### D · LOW · process — prior reviewers' suites keep falling out of the tree
`tests/test_review_2026_08_23.py` (ed1cc34's five repros, all still failing
on master — R1 alias-unanimity, R2 above, R3 stale-train) existed only on a
side branch; run 6's `test_review_2026-08-24d.py` likewise had to be
recovered from history by me last run. Review repros are the repo's defect
ledger; I restored both into this branch. Recommend: findings/review repro
files are append-only and merge trains must never resolve them away.

## 3. WHAT HOLDS UP (attacked, held)

- **seal_unprovable's refusal path survives resume**: `_leaf_from_payload`
  restores `gap_kind` from the checkpoint asdict, so resumed gapped leaves
  are classified identically — the fix was not just live-path.
- **verify_artifact_gate** (A6 wiring): real production caller exists,
  refuses on missing/corrupt bytes, empty refs pass honestly.
- **C1/C3 checkpoint integrity**: absent digest fails, mandatory fetches-key
  enforced, scratch-ledger fresh-run check does not launder — all confirmed
  reading current checkpoint.py.
- **adversary apply_verdict**: subtract-only, floor_conf'd, BLOCKING vetoes —
  held under direct probe.
- **independence normalisation**: `in_family`/`independence_key` collapse
  spellings correctly in both live copies (base.independence_family remains
  dead+un-normalised — pinned by S7).
- **verify-merge.sh within-lineage checks**: ok except pre-existing xgboost
  import noise (still hard-fails bare runs, per run 7's E).

## 4. IS THE WORK ACTUALLY USED? — new inert components found this run

- **CrossRunMemoryStore** (`tools/pipeline/crossrun.py`): constructed by ZERO
  production callers — only engine's optional injection point (default None)
  and tests. The cross-run memory feature, including its gap_kinds record,
  never executes outside tests. Family 1 subshape ("extension point nobody
  extends through"), same shape domain-plugins' R3 found for plugins.
- **tools.calibration**: inert behind ImportError (defect B).

## RANKED SUMMARY

| # | Sev | Defect | Repro |
|---|-----|--------|-------|
| A | HIGH | seal contract mirrors (why.py, calibration/instrument.py) stand on gap-classified leaves | 2 tests, fail |
| B | MED | tools.calibration un-importable since 08-23; inert | test fails w/ ImportError |
| C | MED | S1 CRITICAL (zero-result admission) unfixed after merge train | control, fails |
| D | LOW | reviewer repro suites dropped from tree; restored here | process |

Run:
```
python3 -m pytest tests/test_review_2026-08-25.py -q   # 4 failed (expected)
python3 -m pytest tests/test_review_2026_08_23.py \
  tests/test_review_2026-08-24d.py tests/test_review_2026-08-24e.py -q --runxfail
# prior canaries: still red on master — none of runs 6/7's defects were fixed
```
