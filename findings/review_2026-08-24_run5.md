# REVIEW — 2026-08-24, run 12 (standing review role)

Reviewer worktree: `research`, branch `improve/money-path-landing`
(4bc13c4 + repro commit d1a1396). No production code edited.
Repros: `tests/test_review_2026-08-24_run5.py` (5 tests; 4 fail on this
checkout for the documented reason, the money test passes HERE and fails
on origin/master — that asymmetry is itself the finding).

Prior runs consulted: review_2026-08-23 (run 1), 0824 runs 2/3/11, all on
master at the time of writing — **except that as of this run they are no
longer ON master; see R1.**

## Under review this run

The pm1 merge train: `fix/worldbank-planner` (32ac69e),
`fix/adversary-parse-flakiness` (caeeeb8), `fix/mutation-survivors-r2`
(2ef77f8), landed on origin/master ~17:51, six hours after db08c13
landed the C5 arithmetic-contradiction fix. Question asked: did the train
preserve what master already had?

---

## R1 — CRITICAL: the merge train REVERTED the C5 fix and DELETED the repro suites that documented it

Commit `cd7f068` ("autosave: in-flight work on fix/worldbank-planner",
10:24) is an autosave snapshot whose tree predates the morning's engine
work. Merged verbatim into master by 32ac69e, it silently:

1. **Removed the C5 reconciliation fix** (`_sole_bare_boolean`,
   `reconciliation_failure`, the refusal path binding a verified sandbox
   boolean onto leaf direction — 4c09a7b + db08c13). On post-merge
   master, a leaf can again seal a conclusion that its own verified
   computation contradicts. This was the fix for PATTERNS family 9's
   first live incident (the sealed-wrong-answer class).
2. **Reverted the quant gate** to `produced_quant = sandbox-ok OR any
   digit` — year tokens count as quantitative evidence again,
   re-opening exactly what 38aee73's promoted canary closed.
3. **Deleted** findings/arithmetic_contradiction.md,
   arithmetic_goldens.md, redteam_answer_correctness.md and all three
   review reports (runs 11, 2, 3), plus their repro suites:
   `test_review_2026-08-24.py` (10 failing repros),
   `test_review_0824_audit.py` (17), `test_review_0824_run3.py` (8), and
   `test_redteam_answer_correctness.py`.
4. Reverted the C5-regenerated speed-golden fixtures.

I restored each deleted suite from db08c13 against post-merge master:
**all 35 review repros still fail** — the defects they pin are live;
only the witnesses were removed. The suite got greener by deleting its
failing tests. That is PATTERNS family 7 weaponised by an automated
actor, and it violates BUILD_MANDATE rule 4 in spirit ("nothing automated
may weaken a gate" — the review suites ARE gates).

Root cause shape: autosave commits the working tree of a branch without
regarding what the branch has or hasn't merged; the merge train treats
autosave output like authored work. A snapshot taken mid-rebase/mid-drift
becomes a revert with a plausible message. **New family candidate
(family 10): an automated persistence layer converting branch drift into
silent reverts at merge time.** Hunt rule: before merging any branch,
diff it against its declared base for files it DELETES relative to that
base; a deletion of a file the base never had is a drift artifact until
argued otherwise.

Repro: `tests/test_review_2026-08-24_run5.py::test_c5_*`,
`test_quant_gate_*`, `test_review_repro_suites_present_on_master`,
`test_findings_files_present_on_master`. Restore path: cherry-pick
db08c13's engine.py hunk + `git checkout db08c13 -- tests/test_review_*
findings/review_* findings/arithmetic_*`.

## R2 — HIGH: money-path fixes remain unmerged (family 2 at process scale, third documented instance)

Verified live on post-merge master (clean worktree probes):

```
kelly_full(0.05, 1.91)          == 1.0     # decimal odds -> full bankroll (D1)
kelly_with_push(.54,.04,1.909)  == .0780   # exact push-Kelly is .1344 (-42%)
assess_edge(cents 47/54).ev     == 27.2    # true ≈ 0.277 (~100x inflated)
crossed book (60/61):           kelly None but actionable True  (D7 half-open)
```

`improve/money-path-landing` and `redteam/money-path-deep` carry verified
remediations for every one of these and are still not ancestors of
master (`merge-base --is-ancestor` → NOT-MERGED). Run 3 called this "the
single worst process finding"; it has now recurred across three runs
while the affected code sizes real stakes. The direction of error is, in
every case, toward MORE stake — the thing this architecture exists to
prevent.

Repro: `tests/test_review_2026-08-24_run5.py::test_money_path_fixes_still_absent_from_master`
(passes on my branch, fails on master).

## R3 — What holds up this run

- **The C5 fix itself was real while it existed**: `_sole_bare_boolean`
  probed directly (True/False/trailing-newline correct; prose, numbers,
  YES/NO correctly return None); `test_redteam_answer_correctness` ran
  10 passed / 9 xfailed on db08c13. The quant-gate helpers behave as
  documented ("in 2023 the rate was high" → False).
- **R1 checkpoint durability fix survived the merge** — per-worker
  `cp.save` inside `_retrieve`/`_answer_fresh` intact on post-merge
  master; I mutation-tested it (disabling the answer-stage save flips
  the test to FAIL). 16/16 speed goldens still pass because the goldens
  were reverted consistently — internally consistent, externally a
  rollback (family 9's mirror image).
- **adversary-parse-flakiness and mutation-survivors-r2 are genuine
  improvements** and survived the merge cleanly: one shared JSON parser,
  bounded parse retries with a distinct transport-failure reason string,
  robust extract_json, plus the mutation harness PATTERNS.md asked for.
  Credit where due — these merges carried real fixes; the problem is the
  stowaway, not the cargo.

## Ranked list

| # | Sev | Finding | Repro |
|---|-----|---------|-------|
| R1 | CRITICAL — silent capability regression + evidence destruction | Merge of autosave snapshot cd7f068 reverted the C5/quant-gate fixes and deleted 3 repro suites + 4 findings files whose defects are all still live | `tests/test_review_2026-08-24_run5.py` (4 failing); restored db08c13 suites: 35/35 fail on new master |
| R2 | HIGH — unmerged verified money-path fixes; full-bankroll sizing live on master | improve/money-path-landing & redteam/money-path-deep still NOT-MERGED | same file, money test (fails on master, passes here) |
| R3 | note | Everything else in the train checked out; adversary/model hardening real | direct probes above |

## Blunt calls

1. The merge train has no invariant beyond "merge what exists." An
   autosave daemon committing every 5 minutes guarantees that SOME
   branch tip always contains stale state; merging tips without a
   delete-diff check makes silent reverts a matter of time. R1 is that
   time arriving.
2. Master currently seals conclusions its own sandbox contradicts, sizes
   full bankrolls on misread odds, and has deleted the documents that
   say so. The findings directory now describes a system better than the
   one on master — run 2's sentence, still true, now worse.
3. Recommended immediate actions, in order: (a) restore db08c13's
   engine.py + deleted tests/findings onto master; (b) merge
   improve/money-path-landing (it is tested, pushed, and three runs
   have verified it); (c) add a pre-merge guard: reject any branch diff
   that deletes files present in its merge-base unless the commit
   message names them.
