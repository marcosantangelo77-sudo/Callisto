
---

# speed — run 7, 2026-08-23 late evening (perf/standing-speed-0823-182830, worktree gate)

## Recovery first (again): the stranded lineage is now on this branch

This worktree's branch `perf/standing-speed-0823-182830` sat at the d3merge
point (`41f7163`) with ZERO of the speed lineage. Merged
`origin/perf/standing-speed-0823-180717` (runs 1–6: parallel leaves,
per-source fan-out, R1 checkpoint fix, D3-preserving resume, pooled HTTP
client). 24/24 speed tests pass on the merged tree. Run 5/6's escalation to
LAND the lineage stands — this is now the fourth consecutive run that had to
recover it before doing any measured work.

## BASELINE RE-MEASURED (post-recovery, scripts/profile_pipeline.py)

M=3s/F=0.8s: wall 7.761s · busy 33.34s · 6 model calls · 19 fetches · 2590 B.
Stage table: decompose 3.0s | leaves fetch 15.32 busy / ~1.64s wall | answers
15.02 busy / ~3.0s wall | adversary ~0. Matches run 5/6 numbers exactly —
nothing regressed in recovery. Golden fingerprints + five-question Brier
regression PASS on the recovered tree.

## LIVE PRICING — where the remaining wall actually goes

Measured THIS machine, tonight:
- fresh `hermes -z` process per call (backend=hermes_cli): **11.4s and 20.3s**
  for a one-word reply (cold/warm-ish; ~6–14s typical)
- `hermes proxy start --provider nous` (persistent local OpenAI-compatible
  proxy over the same Portal OAuth): warm calls **1.2–2.4s**; 5-way fan-out
  works (upstream 429s are capacity, retried by _post_with_retry as today)

The pipeline wall is D(decompose) + F(fetch) + A(best-leaf answer) +
V(adversary) ≈ 3 serial model calls + fetch time. With per-call process
startup of ~12s, a 7-call run pays ~84s in pure CLI process startup — the SAME
disease as the original 2,625s baseline, one layer up: the transport was fixed
for HTTP endpoints but backend="hermes_cli" still forks a fresh interpreter
per completion.

## MODULE CLAIMED THIS RUN: `config/providers.yaml` (+ a new test file;
no production code change required — the proxy IS openai_compat)

Fix: declare `ox_alpha_proxy` (backend openai_compat, base_url env-overridable,
default http://127.0.0.1:8645/v1) and insert it ahead of ox_alpha in every
task_class failover list. The hermes_cli path stays exactly as-is for
zero-setup degradation; when the one-process proxy is running, every call goes
over persistent HTTP instead of a fresh fork. The adversary stays its own
call; nothing caches across any cutoff; no gate moves.

Correctness plan: characterization tests pinning declaration + ordering +
degradation; live end-to-end check through ProviderRouter; speed tests green;
Brier regression unchanged.


## MEASURED — live, this machine, tonight

| path                              | per-call latency        | source |
|-----------------------------------|-------------------------|--------|
| hermes_cli backend (fresh fork)   | 11.4s, 20.3s            | `hermes -z` one-word reply, timed twice |
| ox_alpha_proxy (persistent proc)  | 1.2-2.4s warm           | same model via `hermes proxy start` + ProviderRouter._post |
| ratio                             | ~6-15x per model call   | |

A 7-call pipeline run (decompose + 5 parallel answers counted once + adversary)
at the CLI's ~12s/call startup pays ~84s in process startup alone; through the
proxy the same calls are HTTP posts on a warm connection. This is the SAME
defect class as the original 2,625s baseline (fresh process per call), fixed at
the transport layer without touching routing semantics.

Offline harness unchanged: M=3s/F=0.8s wall still 7.73-7.76s (simulated delays
dominate; the fix targets LIVE latency).

## CORRECTNESS

- tests/test_speed_persistent_proxy.py: 8 pins — declaration honesty,
  proxy-ahead-of-CLI ordering in all 8 task classes, ox_alpha stays LAST,
  unresolved-env degradation is byte-identical to pre-run7 routing, _post
  request shape, dead-proxy fast-failover with cooldown.
- Adjacent suites (speed/tier5/oxa/checkpoint/pipeline): 108 passed;
  test_backtest_e2e::test_full_pipeline fails — PRE-EXISTING (run 6 verified
  identical failure on clean dcd27a0).
- Brier regression: five-question golden PASS (unchanged tree semantics).
- Adversary remains its own call; no caching introduced anywhere near a
  cutoff; no gate moved.

## OBSERVATION (not changed — would alter failover semantics)

_post_with_retry retries only >=500 and transport errors. Upstream Portal 429s
(capacity) fail over immediately instead of retrying-in-place. Falsifier: a
run where 429-retry would have succeeded but failover produced a worse answer.
Left to the router owner deliberately.

## NEXT BOTTLENECK

With the proxy running, remaining serial chain is decompose(M) + best-leaf
answer(M) + adversary(M) ≈ 3 model calls + fetch time. Leaf-level pipelining
of retrieval behind answers is NOT safe here: leaf k's answer reads ledger
entries replayed from leaves 0..k-1 (first-wins URL / primary-by-hash), so
starting answers before the full replay can change source-class assignment —
an answer-changing speedup is forbidden. Next priced cost: prompt size of the
answer call (evidence items are 4000-char bodies each), only after re-pricing
with live endpoints per run 5/6's standing instruction.
