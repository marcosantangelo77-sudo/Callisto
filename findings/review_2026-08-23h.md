# REVIEW — standing run 10, 2026-08-23 late (reviewer: ox-alpha)

Worktree `review-ox` on closeout2-equivalent bytes (64e7f87 + review runs
1–9). No production code edited. Reproductions:
`tests/test_review_2026-08-23h.py`, all four executed and shown to FAIL
against the current checkout before commit.

Prior runs consulted: 1–9, most directly run 9 (`review_2026-08-23g.md`,
stranded on origin/review/ox-alpha-0823 — itself an instance of the
meta-defect below).

**Under review this run:** `build/cli-front-door` (~4,000 added lines across
17 commits: schema-seam consumer repair, K2 routing fix, gate-policy
operator gating on autonomous.py/self_repair.py, PredictionStore +
migration 016, Thompson routing policy, wiki/memory pass). No prior run
examined it.

**Families hunted:** #1/#5 (a check or component that cannot influence the
system it claims to protect), #2 (fix lands in one copy while another keeps
the bug — at BRANCH granularity), #6 (direction of rounding error), #7
(tests passing/committed without touching their subject).

---

## WHAT HOLDS UP

### Gate-policy operator gating (a31c708) — good work, correctly scoped
`_phase_refresh_signals` and `_requeue_stale_signal_rejections` now refuse
to rewrite historical evidence / un-reject hypotheses without
CALLISTO_ALLOW_THRESHOLD_MIGRATION. The docstrings name audit mechanism #2
explicitly; the no-flag path logs what it WOULD have done instead of doing
it; `tests/test_gate_policy_startup_rewrites.py` drives the real coroutines
against a real DB with the flag unset. Verified passing on branch bytes.
This closes the last two known gate-rewrite paths and honours hard rule #2.

### K2 question-identity fix (99f222e) — logic verified correct
`scores.aggregate()` dedupes on question_id before shrinkage;
ThompsonRoutingPolicy adds a distinct-question coverage gate so a
cherry-picking model cannot win on a subset mean (was 500/500 per the K2
red team). Its own tests (`test_fix_k2_routing_coverage.py`) pass on branch
bytes, including the 500-decision acceptance repro and an equal-coverage
control that proves the gate isn't just excluding everything. The dedupe is
defence-in-depth behind write-side dedupe in retrodiction batch.py. Sound.

BUT see RV10-B: nothing production-side ever calls it.

### PredictionStore + migration 016 (3a8ab07) — clean as far as it goes
Append-only predictions, explicit overwrite flag for corrections,
fail-closed outcome vocabulary (unknown token raises rather than silently
becoming 'unresolved'). Tests pass. Caveat under "inert" below.

## WHAT DOES NOT HOLD UP

### RV10-A (HIGH) — create_hypothesis dies on every post-013 database, and cli-front-door's own test suite PROVES it (family #2 across branches + #7)

The seam split (17bd962) put migration 013 into master's startup path via
api.py → apply_pending_migrations. After 013, ``hypotheses`` has NO
sport/market_type columns (I reproduced this against the real migration
runner: post-migration columns are [.., domain, ..]; welded INSERT raises
"table hypotheses has no column named sport").

Master's HypothesisManager still INSERTs sport/market_type and its dup-guard
still queries WHERE sport = ?. The writer-side fix exists — e4edcca
("HypothesisManager works on both sides of the schema seam") — but it lives
ONLY on origin/review/rotating-0823-155500. Merged nowhere.

Worse: build/cli-front-door's c91d7c2 ships test_improve_schema_seam.py
whose docstring states finding #1 verbatim ("create_hypothesis INSERTs
sport/market_type, so on any post-013 database it raised no such column") —
and I ran those exact committed tests against those exact committed bytes
via git archive: **10 of 24 FAIL**, including both roundtrip shapes, the
dup guard ("no such column: sport"), fresh-born-seam-shape, and all four
seam-shape consumer pins. The instance committed tests demonstrating its
subject was broken and reported completion anyway. That is family #7 at
its purest: the failing evidence was IN THE REPO.

Reproductions: test_create_hypothesis_survives_migration_013,
test_dup_guard_query_runs_on_seam_shape (both fail here).

Impact: one API restart on the workstation migrates the real DB and kills
hypothesis creation, dup guarding, _days_of_odds_data, list/get sport
resolution. This is the highest-priority merge in the repo, ahead of A2.

### RV10-C (HIGH, prospective) — merging cli-front-door re-imports confidence inflation (family #6)

cli-front-door branched from c2e189d, BEFORE ba0a63c (floor_conf
downward-only quantisation, six call sites) reached master. Branch bytes
contain NO floor_conf anywhere in agp/, and tools/pipeline/engine.py still
quantises three confidence sites with plain round() (lines ~370/411/469),
which can RAISE a score — the exact defect ba0a63c exists to prevent.
Merging this branch into current master without rebasing over ba0a63c
re-imports laundering through the synthesis path. Reproduction:
test_pipeline_engine_does_not_round_confidence_upward (fails).

Also stranded by the same fork: fef694f (critic track record across
restarts), 682e0a0 (memory-laundering digest hole), ba1944f (retire
local_kelly), c2e189d (wiki fail-closed confidence). All reviewed-good work
that only exists on this unmerged line.

### RV10-B (MEDIUM) — ThompsonRoutingPolicy has zero production callers (family #1's cousin: a component nobody calls)

git grep across tools/, api.py, orchestrator.py, scripts/: the ONLY
reference to ThompsonRoutingPolicy outside tests is its definition. The K2
coverage gate, W8 task-class slices and recency weighting are all real —
and reachable by nothing. Scores are recorded into model_scores.jsonl by
run_retro_batch.py; no serving-path decision consults them. Empirical
routing is currently a measurement archive with a decision engine bolted on
that never decides. Same inert status for PredictionStore: migration 016 +
writer shipped together, but grep finds no caller outside resolvers/__init__
and tests. The BUILD_MANDATE called OutcomeResolver the highest-value
change in the repo; its write side is wired to nothing.

Reproduction: test_thompson_routing_policy_has_a_production_caller (fails).

### Also noted (LOW)
- verify-merge.sh fails on this tree: tools/ml_backtest imports missing
  first-party ml_classifier (pre-existing xgboost-artifact family; noted,
  not new).
- The improve_schema.md findings file honestly documents the COALESCE
  static-resolution trap — the prose was right while the pinned contract
  failed. Reading findings without running their tests would have missed it.

## RANKED LIST

| # | Severity | Finding | Reproduction |
|---|----------|---------|--------------|
| RV10-A | HIGH — hypothesis creation dead after next API restart | e4edcca writer fix stranded on review/rotating-0823-155500; master + cli-front-door both keep the bug; cli-front-door's own committed tests fail 10/24 on its own bytes | tests/test_review_2026-08-23h.py::test_create_hypothesis_survives_migration_013 (+dup guard); git archive bcf + pytest test_improve_schema_seam.py |
| RV10-C | HIGH on merge | cli-front-door forked pre-ba0a63c; engine.py rounds confidence upward in 3 sites; merge without rebase re-imports family #6 | ::test_pipeline_engine_does_not_round_confidence_upward |
| RV10-B | MEDIUM — inert capability | ThompsonRoutingPolicy (incl. K2 gate) and PredictionStore have zero production callers | ::test_thompson_routing_policy_has_a_production_caller |

## PROCESS NOTE

Run 9 said drain the merge queue; since then the queue grew by cli-front-
door, redteam/195100, perf/200000+231635, improve/200236. The meta-defect
is now the dominant failure mode of the whole operation: fixes exist,
reviews approve them, and master keeps every bug because the merges stopped.
Recommended order once merges resume: (1) cherry-pick e4edcca or merge
155500 (RV10-A), (2) merge/rebase cli-front-door over ba0a63c (RV10-C),
(3) A2/A14 + sidak scope per run 9, then wire ThompsonRoutingPolicy into
ProviderRouter or stop describing routing as empirical (RV10-B).
