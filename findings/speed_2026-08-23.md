# SPEED FINDINGS — 2026-08-23 (standing role, run 2)

Branch: `perf/standing-speed-0823-150903` (unchanged from run 1).
Prior run (same file, above): parallel leaves, 4.9× on the leaf section,
16/16 golden-fingerprint tests green.

## THIS RUN'S MODULE — announced before edit

**Owner this run: `tools/pipeline/retrieval.py`** — specifically the
per-round source fan-out loop in `IterativeRetriever.retrieve()` (the
`for spec in specs:` block). Nothing else will be touched. The engine's
`_FetchRecorder`/merge machinery is READ-only from here; if the fix needs an
engine seam I will stop and re-announce rather than edit `engine.py`.

Not touched: `tools/pipeline/engine.py`, `tools/pipeline/transport/*`,
`tools/sources/*` (base.py rate limiter is read, not modified), `agp/*`,
`tools/pipeline/checkpoint.py`.

## PROFILE FIRST — fresh end-to-end measurement, post parallel-leaves

`scripts/profile_pipeline.py --model-delay 3.0 --fetch-delay 0.8`
(post-transport-fix inference assumption M=3s/call, F=0.8s/fetch):

    sealed=True leaves=5 fetches=5 conf=0.55 wall=10.162s

| stage              | busy_s | calls | bytes |
|--------------------|-------:|------:|------:|
| decompose          |  3.001 |     1 |     0 |
| leaves(5) fetch    | 15.351 |     0 |  2590 |
| leaves(5) answer   | 15.011 |     5 |     0 |
| adversary(+seal)   |  0.000 |     0 |     0 |

Wall is now 10.16s (was ~37s predicted pre-parallel-leaves; matches).

Exact schedule from the instrumented run (offsets in seconds):

- t=0.00 Architect (decompose) +3.00s            → critical path 3s
- t=3.09–7.14 **19 fetches in 6 SERIAL WAVES**: clinicaltrials×5 →
  federalregister/worldbank ×5 → openalex×3 → semanticscholar×3 →
  wikidata×3. Each wave ≈ F=0.8s → retrieval = 4.06s of wall.
- t=7.15 five Manager answers concurrently (+3.00s) → critical path 3s

**Critical path: decompose 3s + retrieval 4.06s + answers 3s = 10.16s.
Retrieval is 40% of wall and is pure serialization.**

Why serial: each leaf fetches its distinct sources ONE AT A TIME inside
`IterativeRetriever.retrieve()` (`for spec in specs:` — sync urllib through
`RestSource`). Leaves already overlap; within a leaf nothing does. With S≈4
sources/leaf at F=0.8s that is ~3.2s per leaf paid even though every leaf's
sources are independent of each other.

## THE FIX (this run)

Parallelize the per-round source fan-out inside one leaf:
each spec's fetch+gate runs in its own thread via `asyncio.to_thread` /
executor, results are merged back in SPEC ORDER so:

- trace.admitted / rejected / queries / rounds keep exact serial order;
- scratch-ledger record order (replayed by the engine in leaf order) stays
  byte-identical to the serial run;
- `relevant_titles`, refinement, terminator decisions unchanged (they are
  computed AFTER the round completes, from round-ordered results);
- errors: a failed source still just excludes itself and continues —
  exception selection per source is local to that source, no cross-source
  ordering hazard exists;
- `_RateLimiter` is already thread-safe per source spec, so politeness
  intervals are preserved exactly (per-source spacing unchanged; sources
  already fetched concurrently ACROSS leaves, so per-host concurrency does
  not increase beyond what parallel leaves already produce).

Predicted post-fix retrieval: max-over-sources ≈ F + rate-limit spacing ≈
~1s instead of Σ ≈ 4.06s → end-to-end wall ≈ 7s vs 10.16s (~1.45×) at
M=3s/F=0.8s; grows with source count and F (live HTTP F≫fixture F).

## CORRECTNESS PLAN

1. Extend the golden-fingerprint suite: same scenarios, plus a multi-round
   case; fingerprints must stay byte-identical (ledger_primary hashes,
   admitted/rejected order included).
2. Wall-clock test: N-sources-per-leaf sublinear in source count.
3. Brier regression on the five scored data/retro_batch questions must
   match to 1e-9.

---

## RUN COMPLETION — run 2, later 2026-08-23

The fix landed as c6a0a03 on `perf/standing-speed-0823-150903`, pushed.

### Measurement — the ratio

Same instrumented profile, M=3s/F=0.8s, 5 leaves, 19 fetches:

| variant            | wall      | retrieval span |
|--------------------|----------:|---------------:|
| serial per-source  | 21.42s    |        15.36s  |
| parallel fan-out   | **7.72s** |       **1.63s**|

**2.8× end-to-end; retrieval section 9.4×.** (Serial baseline is the same
code path with `ThreadPoolExecutor(max_workers=1)` injected, so the
comparison is exact.) End-to-end across the standing role so far: ~37s →
10.16s → 7.7s predicted live wall for a 5-leaf question at
post-transport-fix latencies.

### Correctness — VERIFIED

- 33/33 across `test_speed_parallel_leaves.py` (12 golden-fingerprint
  scenarios + five-question Brier regression to 1e-9),
  `test_speed_parallel_sources.py` (new: spec-order under REVERSED
  completion delays, ledger determinism under concurrency, in-flight
  overlap probe, sublinear wall), and `test_build_w1_retrieval.py`.
- Order-preservation mechanism: fetches are COLLECTED concurrently
  off-thread (each with a private `_FetchRecorder`; RestSource provenance
  writes never touch shared state), then PROCESSED in spec order —
  admitted/rejected/rounds/queries/ledger-replay byte-identical to serial.
- `_RateLimiter` untouched: thread-safe per source, politeness spacing
  unchanged; cross-source concurrency bounded at what parallel leaves
  already produce.
- No caching added anywhere near a cutoff. Adversary still its own call.
  Nothing can raise confidence.

### Pre-existing failure found (NOT mine — documented, not fixed)

`tests/test_review_checkpoint_partial_progress.py::test_crash_during_answers...`
fails at HEAD~ too, i.e. WITHOUT this run's change. This is reviewer finding
R1 in `findings/review_2026-08-23.md`: the parallel-leaf restructure moved
answer-checkpoint saves behind phase-wide success, so a mid-phase crash drops
completed leaves' checkpoints. Fix direction is written there (save inside
`_answer_fresh`). It lives in `engine.py`, which I do not own this run.

### Next bottleneck (for run 3)

Critical path now: decompose 3s + answers 3s + retrieval 1.6s = 7.7s.
Retrieval fell from 40% to ~21% of wall; remaining serial costs are single
structural model calls. Before choosing a target, next run should re-price
with REAL transport F (live fetch latency ≫ fixture) — if F is large,
multi-round retrieval still dominates and round-level pipelining is worth
more than prompt-shrinking.
