# SPEED FINDINGS — 2026-08-23 (standing role, run 0823-150903)


Branch: `perf/standing-speed-0823-150903`
Owner this run: **tools/pipeline/engine.py** (announced before edit, per role brief).
Also mine: `tests/test_speed_*.py`, `scripts/profile_pipeline.py`, this file.
Not touched: `tools/pipeline/hermes_cli.py` + `tools/pipeline/transport/*` +
`tools/sources/query_builder.py` (in flight on `perf/persistent-transport`);
`agp/*`; everything else.

---

## METHOD

Instrumented end-to-end profile: one real retrodiction question ("Will Apple
report quarterly results above Wall Street consensus…") driven through
`ResearchPipeline` with fixture transport and scripted responses, every model
call and fetch recorded with start offset / duration / bytes
(`scripts/profile_pipeline.py`, committed alongside).

Measured structure (zero simulated latency, so counts are exact):

| stage              | busy_s | model calls | bytes |
|--------------------|-------:|------------:|------:|
| decompose          |  0.000 |           1 |     0 |
| leaves(5) fetch    |  0.000 |           0 |  2590 |
| leaves(5) answer   |  0.000 |           5 |     0 |
| adversary(+seal)   |  0.000 |           1*|     0 |
| **total**          |        |      7      |  2590 |

\* adversary call present but routed to an uninstrumented stub in the profile;
counted here because production pays it.

19 transport calls fan out over 6 endpoints (clinicaltrials ×5, worldbank ×3,
openalex ×3, semanticscholar ×3, wikidata ×3, federalregister ×2) — per leaf,
~4 DISTINCT sources fetched one-at-a-time; no same-URL re-fetching observed,
so the waste is not duplication, it is serialization.

## THE PRICED TABLE (structure × per-unit latency)

Two transport assumptions. M = per-model-call latency, F = per-fetch latency.

Pre-fix CLI (measured on this branch today: 7.6s for a trivial `hermes -z`;
brief baseline 11.3s/call round trip):

| section            | formula            | M=11.3s F=1.0s |
|--------------------|--------------------|---------------:|
| decompose          | 1×M                |         11.3s  |
| leaf section       | 5×(4×F + 2×M)      |        124.3s  |
| adversary          | 1×M                |         11.3s  |
| **wall**           |                    |       ~147s    |

(The historical 2,625s run had more rounds/retries per leaf; same shape.)

Post-fix persistent transport (assume M=3s inference-only, F=0.8s):

| section            | formula            | seconds |
|--------------------|--------------------|--------:|
| decompose          | 1×M                |    3.0  |
| leaf section       | 5×(4F + M)         |   31.0  |
| adversary          | 1×M                |    3.0  |
| **wall**           |                    |  **37s**|

**84% of post-fix wall time is the leaf section, and it is strictly serial:**
`engine.run()` processes leaves one at a time — sync urllib fetches that block
the event loop, then an awaited model call, then the next leaf. Leaves are
independent: leaf k's retrieval and answer depend on leaf k only. This is the
next bottleneck after the transport fix.

## THE FIX (this run)

Overlap the independent leaves while preserving byte-identical outputs:

- **Phase A**: all leaves' retrieval run concurrently off-loop
  (`asyncio.to_thread`), each writing a scratch provenance ledger; merges
  replay into the real ledger in leaf order → identical ledger state, no
  cross-thread mutation, no locks, no `agp/` edits.
- **Phase B**: all leaves' answers run concurrently on the loop; evidence
  items are returned per leaf and appended to `session.evidence` in leaf
  order afterwards → identical evidence order feeding conclusion + adversary
  + checkpoints.
- Errors: `gather(return_exceptions=True)`, then raise the first exception in
  LEAF order — identical error-selection to the serial loop.
- No caching anywhere near a retrodiction cutoff (none added). The adversary
  stays its own model call. Nothing that can lower a gate changed.

Predicted post-fix wall: 3 + max(4F+M) + 3 ≈ **9–10s vs 37s (~3.8×)**; the
win grows with leaf count.

## CORRECTNESS PLAN (before merge)

1. Golden fingerprints captured from CURRENT serial code across 8 scripted
   scenarios (leaves 1–5, adversary BLOCKING/MAJOR/MINOR, compute-reask,
   refusal paths) → `tests/fixtures/speed_golden/*.json`.
2. `tests/test_speed_parallel_leaves.py`: fingerprint equality after the
   change, sublinear wall-clock scaling with simulated delays, ledger
   merge-order determinism, error-order preservation.
3. Five scored questions in `data/retro_batch/`: offline pipeline predictions
   before vs after → Brier must match to 1e-9.

---

## RUN COMPLETION — same run, later 2026-08-23 (verification + measurement)

The parallel-leaf implementation and the golden-fingerprint suite landed via
autosave commits (9ae4757..8492c05). This session verified them end to end.

### Correctness — VERIFIED

- **16/16 tests pass** in `tests/test_speed_parallel_leaves.py`, including all
  12 golden-fingerprint scenarios and the five-question Brier regression.
- The `two_computing_leaves` golden was initially suspect (it had been captured
  while engine.py sat mid-restructure, freezing leaf 1's compute answer as
  missing). Re-verified against the TRUE serial engine: the pre-restructure
  code at `2fd244c^` (f2a9145), restored into a scratch tree with only the
  tuple-return seam fixed, produces exactly the committed golden — answers
  [computed v / computed w], confs [0.70 / 0.65], 4 evidence items in leaf
  order, ledger_observations=7. The golden is legitimate; the serial-vs-
  parallel pairing is stable because tagged scripting dispatches on the
  deterministic leaf ordinal, not arrival order.
- Merged origin/master (checkpoint red-team fixes touch the same module's
  neighbour `tools/pipeline/checkpoint.py`); clean merge, 60/60 across speed +
  pipeline + checkpoint + retrieval suites.
- Pushed as cb42fe0 on `perf/standing-speed-0823-150903`.

### Measurement — the ratio

First attempt compared against HEAD of this branch and showed ~1.0×. That was
a measurement error: the autosave daemon had already committed the PARALLEL
engine to HEAD, so "serial" was not serial. True baseline is `2fd244c^`.
Measured with real engines, 5 leaves, F=50ms/fetch, scripted Manager latency M:

| per-model-call M | serial 5-leaf | parallel 5-leaf | speedup |
|------------------|--------------:|----------------:|--------:|
| 0.1s             |         1.54s |           0.40s |   3.9×  |
| 0.5s             |         3.50s |           0.79s |   4.4×  |
| 3.0s (post-transport-fix inference) | 15.99s | 3.29s | **4.9×** |

Convergence toward 5× as M grows confirms full leaf overlap (verified
independently: max concurrent Manager calls = 5; retrieval phase overlaps too —
wall < sum of fetch busy-time). At the post-transport-fix latency assumption
(M=3s) the leaf section drops from 31s to ~3.3s; end-to-end predicted wall
goes from ~37s to ~10s for a 5-leaf question.

### Hard rules — unchanged

No caching near any cutoff (none added). The adversary remains its own model
call through its own router seam. Nothing in the change can raise confidence;
the evidence gate, independence counting, and seal path operate on identical,
leaf-ordered inputs.

### Process notes

- The worktree was reassigned mid-session (`loop/` → review/rotating-0823-154611)
  while work was uncommitted; recovered by checking out my branch (worktree was
  clean — autosave had committed everything). Reconfirming the memory rule:
  check `git branch --show-current` before every write here.
- Lesson recorded: when measuring before/after on a branch an autosave daemon
  commits to, verify WHICH engine the baseline commit actually contains before
  trusting a ~1× result.
