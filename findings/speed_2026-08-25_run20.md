# SPEED run 20 — 2026-08-25 · branch perf/standing-speed-0825-run20 (worktree money)

Recovery: worktree money was on perf/standing-speed-0825-run19 at fe3d703
(run 19 tip, clean). Opened perf/standing-speed-0825-run20 from it.

## PROFILE FIRST

Offline structural (`scripts/profile_pipeline.py --model-delay 3.0
--fetch-delay 0.8 --leaves 5`): sealed=True, wall 6.964s, busy 30.057s,
6 model calls, 15 fetches, 1445 B — byte-identical to runs 5–19. Nothing
regressed upstream of the batch seam.

Pricing the batch seam with tonight's warm-proxy numbers (~4.5s/call,
~6 calls/question): a serial 5-question batch pays ~135s of pure serial
wall. Run 19 landed `BatchConfig.max_concurrency` (measured 2.50x at mc=3)
but deliberately left `scripts/run_retro_batch.py` — THE scored path —
serial pending a live checkpoint-safety verification under load. That
verification is this run's work; until it lands, every scored batch keeps
paying the 2.5x.

## MODULE CLAIMED THIS RUN: `scripts/run_retro_batch.py` ONLY

(+ new pins in tests/test_speed_batch_concurrency.py and this findings file.
tools/retrodiction/batch.py is NOT touched — run 19's machinery is used as-is.)

Change: the scored batch constructs BatchConfig(max_concurrency=3) by default
(CLI-overridable via --max-concurrency). Contract inherited from run 19:
per-question body unchanged; appends are synchronous blocks on the event-loop
thread; checkpoint saves are atomic (tempfile + os.replace); resume/error-row
semantics untouched. NO caching, nothing crosses a retrodiction cutoff, the
adversary stays its own model call (concurrency shares transport objects only).

Falsifier: live mc=1 vs mc=3 batches whose result rows / Brier differ in any
byte, or whose walls do not drop ~min(N,mc)x.

## MEASURED (filled in below)

## CORRECTNESS VERIFICATION (filled in below)
