# REVIEW 2026-08-23c — standing review role, run 3

Reviewer: standing review agent (branch `review/rotating-0823-155500`,
base = master @ 5e88b05). **No production code was edited.** Defects are
`xfail(strict=True)` reproductions in `tests/test_review_2026-08-23c.py`;
controls in the same file prove each xfail fails for the stated reason.

Prior runs consulted: run 1 exists only on `review/rotating-0823-132703`
(never merged); run 2 on `review/rotating-0823-150721` (never merged). Both
of their open findings were re-verified against today's master before being
re-recorded here — see P0.

## Scope

| Work | Commits | What was checked |
|---|---|---|
| W5 cutoff-signature fix | 5d59ad4 | claim vs code; is the check now actually on; who signs |
| C1+C2 resume guard | a7b951e | re-audit of seal_guard/replay_ledger; run-2 P1 pin still missing on master |
| C3 vacuous-guard + C4 age forgery | a6466bc | claim vs code; keyed-regime reality check |
| "Six inflation paths closed" | ba0a63c, 96be252 | grep for OTHER round() clamp sites (mandate item 3) |
| Prose-direction defect record | 5e88b05 | does the claimed repro file exist and xfail |
| verify-merge.sh mechanical pass | current HEAD | mandate item 4 |

## What holds up

1. **W5 (5d59ad4) is real and correctly wired at both ends.** `CutoffEnforcer`
   now resolves its key from `harness_key()` by default, refuses unsigned
   proofs unless `allow_unsigned=True` at the call site, and wayback signs by
   default under the same resolver. The docstring's fail-closed policy now
   matches the code.
2. **C1/C2/C3 production behaviour survives re-audit.** Absent/empty digests
   are integrity failures; the dedup key can't collapse distinct records;
   fresh runs check via `_ScratchLedger`; resumed runs replay only
   `ck.run == trace.run`. `_is_fetch_stage` centralises the membership rule
   that previously landed three times.
3. **The floor_conf consolidation is broader than run 2 credited.** Grep for
   remaining upward-capable `round()` clamp sites across agp/tools found
   exactly two (R5 below, plus the ≤5e-05 site run 2 already noted).
4. **verify-merge.sh**: no deleted public functions, no vanished tests, no
   shrunken files vs HEAD. The one import failure (`tools.ml_backtest` →
   `joblib` missing) reproduces identically on clean master — pre-existing,
   exactly as findings/verify_merge_ckpt_resume_gate.md triaged.

## What does not — ranked

### P0 — both prior reviews are unmerged; their defects are unrecorded anywhere an operator looks

Run 1 (`review_2026-08-23.md`, four xfails) and run 2
(`review_2026-08-23b.md`, R5 + the missing C2 pin) live only on review
branches. Master carries none of them. I re-ran run 2's own test file
against master: 5 passed / 3 xfailed — every finding still open. This is
the third consecutive occurrence of the pattern run 2 named: *reviews don't
merge*. A review that doesn't land has reviewed nothing; this file may
share its fate, so the highest-value item remains process, not code.

### P1 — F5 never closed: mixed-provenance synthesis still scores VERIFIED off one PRIMARY voice

The red-team's original finding list (findings/redteam_confidence.md, F5)
said synthesis "rides the strongest member's ceiling". ba0a63c's commit
message claims six paths closed with "ONE shared quantiser used by every
site" — but `tools/pipeline/synthesis.py:175-177` still computes
`best_class = max(...)` over items and `confidence_from_agreement`
(line 309) grants the WHOLE group that ceiling:

    triangulate([PRIMARY bls + INFERRED gossip + INFERRED mirror]) → score == 1.0

Verified live on master: three voices whose majority provenance is
INFERRED gossip score VERIFIED (1.0), above even the SECONDARY ceiling,
because one item declared PRIMARY. This is precisely red-team finding F5,
still present, while the commit narrative says the family is closed. The
shipped laundering test pins it as expected-broken but nothing tracks it
as an OPEN defect — it reads as history, not backlog. Repro:
`TestP1SynthesisMixedProvenanceStillVerifies` (xfail strict).

### P2 — F6a/F6b/F6c also still open, and the engine path still can't engage the self-review cap

Same pattern as P1. On master:
- `engine.py:577` calls `self.adversary.attack(...)` and then
  `Adversary.apply_verdict(clamped, objections)` — objections only. No
  `ReviewProvenance` is ever built on this path, so `SELF_REVIEW_CEILING`
  (0.54) is structurally unreachable in the main pipeline even though the
  engine prints a note claiming it "is capped (SELF_REVIEW_CEILING)".
  The note is false advertising: the cap applies only via
  `AdversaryPanel.apply_panel_verdict`, which no production caller uses
  (grep: AdversaryPanel referenced only by tests).
- `PanelVerdict(objections=[], backend_failures=3).apply(0.99) == (0.99, "")`
  verified live — a panel whose critics ALL failed clamps nothing. F6c
  unchanged.
Repro: `TestP2SelfReviewCapUnreachableInEngine` (xfail strict).

### P3 — C4's signed-age regime is inert in practice: nothing configures a key

C4's fix is well-built (HMAC over canonical payload including produced_at;
gc reads authenticated age; forged freshness ⇒ infinite age ⇒ collected).
But it only operates when `CALLISTO_CUTOFF_KEY`/`CALLISTO_SEAL_KEY` is set,
no launcher or script on this machine sets either (grep over repo + env),
and all 19 checkpoints in the live state dir are unsigned. Same for W5's
cutoff signatures: correct code, zero signed records in existence. The
failure mode is the project's signature move — a guard that is ON in tests
and structurally OFF in deployment. Not a code defect; an operability gap
that makes C4/W5 currently decorative outside pytest.

### R5 (re-recorded from run 2) — claims.recompute_confidence rounds UP to the ceiling

`agp/claims.py:141,145`: `recompute_confidence([SECONDARY], 0.7499) == 0.75`
and `recompute_confidence([INFERRED], 0.5451) == 0.55`, both RAISED past a
tier boundary — the same shape floor_conf was created to kill, on the
long-lived-claims BeliefRecord path where raised values are hash-chained
into history. Run 2's xfail repro still xfails on master. Repro:
`TestR5ClaimsClampRoundsUp` (re-pinned).

### R8 (new, minor) — relevance gate passes semantically-empty content on short-word overlap

Property sweep (3,000 random cases): the gate's coverage invariant holds
exactly — admitted iff coverage ≥ min_coverage. But prefix matching means
an unrelated article containing generic short words ("states", "supply",
"the chain") covers 50–67% of a semiconductor question's tokens and is
admitted at the production default (0.25 — trivially low; tests use 0.25–
0.9 but never notice the default differs from what they exercise). This is
a weaker cousin of the MORNING_REPORT lexical-selection finding: the gate
measures word overlap, not relevance, so "retrieval failure" evidence can
still enter ingestion — the exact failure the second live run's adversary
objected to ("selected by string coincidence"). Lower stakes than P1/P2
because rejection reasons ARE recorded and the adversary remains a backstop.
Repro included as a passing characterisation test, not an xfail.

### R9 (process) — run 2's P1 regression pin still absent from master

Run 2 added `TestC2ScopePinOnResumedBranch` because deleting seal_guard's
run-filter keeps every shipped C1/C2 test green. That pin exists only on
the unmerged run-2 branch. Until it merges, the C2 scope fix remains one
careless refactor away from silently regressing. Re-included here verbatim
in spirit (see `test_c2_scope_filter_is_load_bearing_source_pin`).

## Inert / unwired (mandate item 5)

- `AdversaryPanel` / `apply_panel_verdict` / cross-model independence
  machinery: no production caller. The entire F6 fix surface is dead code
  from the pipeline's perspective.
- Checkpointing: engine accepts an injected checkpointer; the only
  production construction site (`scripts/run_retro_batch.py`) is the
  retrodiction batch, not the research pipeline. Live state dir has
  checkpoint files (from tests/manual runs) but no wired pipeline writes them.
- Signed-age + cutoff signing (see P3): correct, unused without env keys.

## Not audited this run (next-run queue)

- build/dd-decomposition-diversity (~85 commits) before any merge.
- findings/i3_synthesis.md claims vs tools/pipeline/synthesis.py behaviour.
- Whether ANY prior review branch ever merged (check after next merge train).

## Verdict

W5 and C1/C2/C3 hold up under adversarial re-reading — these are genuine
fixes with honest tests. But the inflation family is NOT closed: the
commit that declared victory fixed rounding while leaving the two biggest
holes (F5 synthesis max-ceiling, F6 unreachable self-review cap) open, and
their repro tests sit in a file that reads as historical documentation
rather than an open-defect ledger. And for the third run running, the
reviews themselves aren't merging. Fix the merge pipeline before fixing
anything else; everything else is already written down and failing loudly.
