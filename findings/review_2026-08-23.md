# Review — ox-alpha, 2026-08-23 (first run of the standing review role)

Scope: commit `fa2bea9` ("declared stance", on origin/master) and unmerged
branch `fix/confidence-laundering` @ `156a837`. No prior findings/review_*.md
existed; nothing had been reviewed before this run.

Method: read both diffs in full, re-ran the targeted red-team suites at the
reviewed heads, probed the fixed code paths with constructed inputs, and grepped
for second copies of every rule touched.

## What holds up

1. **fa2bea9's central claim is TRUE and verified.** `_leans_yes` is gone from
   `tools/pipeline/retro.py`; stance is declared by the model, carried on
   `LeafOutcome.stance` / `PipelineResult.stance`, and UNDETERMINED maps to
   p=0.5 with no lean. `tests/test_redteam_direction_from_prose.py`: 6/6 pass
   at fa2bea9. The instrument mirror (`sign_of_prediction`) reads the declared
   stance and labels it ("declared AFFIRMS" / "declared UNDETERMINED … p=0.5").
2. **fix/confidence-laundering's accounting is accurate.** Its findings file
   claims the laundering block goes 7 → 2 with two contradictory tests left red.
   Measured: exactly that. 15/17 pass at 156a837; the two failures are precisely
   `test_synthesis_best_class_laundering_in_group` (asserts score==1.0 AND
   <=0.75) and `test_panel_verdict_blocking_veto_returns_rounded_up_score`
   (asserts out==0.84 AND <=0.836). Both are unsatisfiable as written; the
   findings file says so and argues the correct post-fix forms. Honest.
3. **A1/A2 do what they claim.** Probed directly:
   `ReviewProvenance(author_model="", reviewer_models=["gpt-4o"]).independent`
   → False; alias reviewer (`gpt-4o-proxy-alias` vs author `gpt-4o`) → False;
   `_same_weights` also catches suffixed variants (`gpt-4o-proxy-v2`).
4. **A3's per-class accounting is sound.** The repro group scores 0.70 (≤
   SECONDARY's 0.75); single-class behaviour unchanged.

## What does not

### R1 (HIGH) — the F6b fix half-landed inside its own file
A2 added `_same_weights()` and used it in exactly ONE property
(`ReviewProvenance.independent`). Two sibling comparisons of the same rule in
the same file still compare raw spellings:

- `agp/ensemble.py:200-201` — `PanelVerdict.unanimous_unrebutted` builds its
  "independent" list with `normalize_model(m) != normalize_model(author_model)`
  and no empty-author guard.
- `agp/ensemble.py:164` — `_indep_attackers` (DisagreementRecord builder), same
  spelling comparison, no empty-author guard.

This is not theoretical. Live repro at 156a837:

    author=gpt-4o, reviewers=[claude, gpt-4o-proxy-alias], both objected
      -> provenance.independent == True          (correct)
         unanimous_unrebutted == True            (WRONG)
         apply(0.9) -> 0.5, "unanimous objection across 2 critics"

The panel counted TWO independent critics when one of them was the author
speaking through an alias, and applied UNANIMITY_BONUS_PENALTY (-0.10 extra)
on the strength of the author attacking itself. The exact laundering A2 was
written to close, surviving in the unanimity path of the same module. The
empty-author variant is masked today only because `unanimous_unrebutted` gates
on `prov.independent`, which A1 fixed first — a fix ordering accident, not a
design guarantee. Test written: tests/test_review_2026_08_23.py::R1.

### R2 (MEDIUM, pre-existing) — tools/calibration is un-importable; ablate.py is inert
Since autosave commit `7e3d007` (before fa2bea9), `tools/calibration/__init__.py`
imports `replay_chain` from instrument.py, where no such name exists. `import
tools.calibration` raises ImportError; `tools/calibration/ablate.py`, whose
whole purpose is importing from the package, cannot be imported by anything.
No test imports either (grep confirms zero test references). This is the
"empirical routing / per-(model,role) scores feed routing" component described
in MORNING_REPORT wave 4 — it is dead code behind a broken door. Nothing fails
in CI because nothing reaches it. Repro: `python3 -c "import tools.calibration"`.

### R3 (LOW) — fa2bea9's instrument mirror retains a vestigial reference
`tools/calibration/instrument.py:190` calls
`retro_bridge.PipelineResearcher._leans_yes(conclusion)` for its disagreement
report. At fa2bea9 retro.py deleted `_leans_yes`, so this line can only work if
a separate bridge module still carries a copy of the DELETED keyword scan.
That would be a third copy of the membership-rule failure pattern (the scan
deleted in one place, mirrored elsewhere). Verify `tools/calibration/bridge.py`;
if the bridge copy remains, any future edit to scoring rules must grep bridge
modules too, or the instrument will explain mechanisms that no longer run.

### R4 (NOTE) — contradictory red-team tests are a recurring shape
Two of the seven laundering-block failures were self-contradictory test files,
not production bugs. The red-team convention ("demonstrate the defect") needs a
rule: a demonstration assertion must be separated from an invariant assertion,
or every fix will be forced to choose between leaving red and editing the file
its mandate forbids touching.

## Ranked

1. R1 — unanimity bonus fires on self-review through an alias (live repro,
   -0.10 confidence penalty from the author's own voice).
2. R2 — calibration/routing package broken + inert since 7e3d007.
3. R3 — possible surviving copy of the deleted keyword scan in bridge.py.
4. R4 — process note on contradictory red-team tests.

## What I did NOT verify
The full suite (targeted subsets only, per role brief); the historical live-batch
numbers quoted in fa2bea9's message; anything outside the two reviewed units.
