# speed — run 6, 2026-08-23 evening (perf/standing-speed-0823-180717, worktree money)

## Recovery first: the stranded lineage is now on this branch

This worktree started at d3merge (`41f7163`) with **zero** of the speed lineage.
Fast-forwarded to `dcd27a0` (run 5's merged tree: parallel leaves + per-source
fan-out + R1 checkpoint fix + D3 admissible-replay guard). 20/20 speed tests
pass on the recovered tree. Pushed. Run 5's escalation stands: land
`perf/standing-speed-0823-175640` / `-180717` as one unit before the next
standing task fires.

## MODULE CLAIMED THIS RUN (before any edit): `tools/pipeline/model.py`

Run 3–5 all concluded the remaining structural cost is model-call COUNT /
prompt SIZE once transport is persistent. `tools/pipeline/model.py` is where
prompt assembly and call dispatch live; I own it this run. Nothing else will be
edited.

## Plan

1. Profile with scripts/profile_pipeline.py at M=3s/F=0.8s for a comparable
   baseline; then re-price with real transport if reachable.
2. Largest single cost gets one fix.
3. Brier + golden-fingerprint regression before declaring victory.

## Log

- (appended below as work lands)
