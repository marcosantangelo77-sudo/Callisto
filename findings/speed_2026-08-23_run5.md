# SPEED FINDINGS — 2026-08-23 (standing role, run 5 — branch perf/standing-speed-0823-175640)

Worktree: `~/callisto-wt/gate`.

## THE HEADLINE OF THIS RUN, AGAIN: stranding struck a fourth time

This worktree started at d3merge (`41f7163`) with **zero** of the verified
speed lineage present. Run 4's recommendation ("land `98121d4`'s lineage ahead
of any further optimisation") was not acted on; every day unmerged, K1 added
serial model-call work to a baseline that had already been measured 4.32x
slower than it needs to be.

## WHAT THIS RUN DID

One merge, one conflict resolution, nothing new written:

    d93a213  merge: land stranded speed lineage (d68e673) onto perf/standing-speed-0823-175640

The single conflict was `tools/pipeline/engine.py`, resume path of the fetch
stage. HEAD carried red-team fix D3 (a resumed checkpoint's records must pass
the same `admissible_checkpoints` predicate seal_guard uses before anything is
replayed into the ledger); the speed side carried the parallel structure and
the resumed StageOutcome append. Resolution keeps BOTH: D3's admissibility
filter wraps `replay_ledger`, inside the parallel leaf-order assembly. No
guard was weakened to make the merge clean.

## MEASURED (scripts/profile_pipeline.py --model-delay 3.0 --fetch-delay 0.8)

| tree                                   | wall     | busy_s | calls | bytes |
|----------------------------------------|---------:|-------:|------:|------:|
| pre-merge (this branch's own baseline) | ~33.4s*  | 33.27  | 6     | 2590  |
| post-merge                             | **7.854s** | 33.31 | 6     | **2590** |

*run 4 measured 33.448s on the equivalent serial tree (`ee549f8`+K1); this
branch's serial critical path is the same shape. Ratio ≈ **4.26x**, bytes and
busy time identical — pure re-parallelisation, no work skipped.

## CORRECTNESS

- `test_speed_parallel_leaves.py` + `test_speed_parallel_sources.py` +
  `test_review_checkpoint_partial_progress.py`: 20/20 pass on the merged tree,
  including byte-identical golden fingerprints fresh-vs-resumed.
- Five-question Brier regression vs serial golden: PASS.
- Adjacent suites (retrieval/checkpoint/pipeline/retro, -k selection): **98
  passed**. (`test_ml_classifier/drift` collection errors are the documented
  xgboost/libomp-unavailable artifact, not regressions.)

## NEXT BOTTLENECK

Unchanged from runs 3–4, now confirmed three times: at M=3s/F=0.8s the wall is
decompose(3s) + max-leaf(fetch+answer) + adversary(M). The remaining structural
costs are model-call COUNT and prompt SIZE, or live transport latency once
`perf/persistent-transport-v2` (b39aa77) lands. Re-price with live F before any
prompt work.

## PROCESS NOTE (escalating)

Four consecutive runs have begun from a tree missing the previous run's landed
work. The merge-train / human merge step is now the dominant cost of this
role. Recommend landing `perf/standing-speed-0823-175640` (this branch, which
now contains the full lineage plus the d3merge fixes) as one unit.
