# SPEED run 18 — findings (2026-08-25, perf/standing-speed-0825-002506, worktree money)

## Recovery

Worktree `money` had been reassigned to `redteam/source-registry-0825` while I
worked (caught by the standing pre-write `branch --show-current` check). The
fresh branch `perf/standing-speed-0825-002506` opened with autosave commit
`ed6bdc0`, which REVERTED the entire runs 9–17 speed lineage (−1,843 lines:
findings, tests, run-9 proxy default, run-17 router-health persistence) and
added only throwaway `_rt_probe*.py` scripts plus a regenerated golden fixture.
Per the `git log -S --all` rule, nothing needed re-implementing: reset onto
run-17 tip `f6fc65d`, pushed. 116 speed/tier5 tests green on the restored tree.

## PROFILE FIRST

Offline structural (`scripts/profile_pipeline.py --model-delay 3.0
--fetch-delay 0.8 --leaves 5`): sealed=True, wall 6.936s, busy 30.070s,
6 model calls, 15 fetches, 1445 B — byte-identical to runs 5–17; nothing
regressed.

Live transport pricing tonight (proxy up on 8645/8646):

| leg | wall | notes |
|-----|------|-------|
| fresh-process routed call | 3.3–10.1s | run-17 health memory working: gpu1 dead hop skipped with ZERO probes |
| one cold first-call outlier | 43.4s | Portal capacity window, not transport |

The per-call transport path is clean. So I priced the next candidate from the
run-7 handoff: answer-stage prompt size.

**Prompt-size A/B — measured and REJECTED as a fix**:
`scripts/live_price_prompt_size.py` (new), interleaved S(1×4000-char body)
vs B(5×4000) through the live router, 9 paired legs across 3 runs:
S mean 15.9s / median 12.7s vs B mean 15.3s / median 11.2s. A 5x evidence
payload is INVISIBLE under Portal's ±10s latency noise. Honest verdict: not
measurable tonight → not claimed. (Context: openalex limit=3 bodies are ~59KB
but the engine already caps each evidence item at 4,000 chars.)

## THE FINDING — the scored batch still forks a fresh CLI process per model call

`scripts/run_retro_batch.py` — THE five-question Brier-scored batch — hardwires
`HermesCliModel`. Every model call shells out `hermes -z`: measured **16.2s**
on this machine tonight (27.8–42.2s under upstream flakiness), of which ~10.6s
is interpreter startup. That is the original 43-minute baseline's dominant
term, still alive on this path, while the router's persistent ox_alpha_proxy
tier (warm 1.2–2.4s measured in run 7; 4.1–5.5s median tonight) sat idle.

## MODULE CLAIMED THIS RUN: `scripts/run_retro_batch.py` + `tools/pipeline/model.py` (RouterModel schema forwarding) ONLY

1. `_make_model()` transport selection: `CALLISTO_RETRO_TRANSPORT` env,
   default **proxy** = ProviderRouter + RouterModel when ox_alpha_proxy
   resolves; honest fallback to the CLI path when unresolved/unavailable or
   explicitly set to `cli`. Same fail-open-to-CLI shape as routing.
   The adversary rides the same selected backend object — still its own call.
2. `RouterModel.complete` now FORWARDS the caller's JSON schema (it silently
   dropped it before), so schema-bearing calls keep upstream structured-output
   enforcement on endpoints that declare the capability.

Correctness: both transports serve the SAME stealth/ox-alpha via the SAME
Portal OAuth — only WHERE the identical completion is served differs. No
caching; no cutoff crossing; gate thresholds untouched.

## MEASURED BEFORE/AFTER (live, interleaved through the batch's own seam)

| transport | legs | median |
|-----------|------|--------|
| proxy (new default) | 4.07 / 5.53 / 35.35s | **5.5s** |
| hermes CLI fork (old) | 42.24 / 8.03s | **25.1s** |

Per model call ≈ 4.5x on medians. A 6-call pipeline question avoids ~6 × 16s
of pure fork tax whenever the proxy is warm; worst case stays bounded by
run-15's patience policy. New pins: tests/test_speed_batch_transport.py (5).

## CORRECTNESS VERIFICATION

- Full speed/tier5 sweep after landing: **121 passed** (was 116 + 5 new).
- Five-question Brier golden (`five_question_brier.json`): **PASS at 0.25**.
- Adjacent suites (parallel leaves/sources, i4 retro batch, r1 scoring):
  50 passed.
- Offline structural fingerprint byte-identical pre/post change.

## NEXT BOTTLENECKS

1. Portal capacity — still dominant external term (35s windows observed).
2. Batch-level parallelism across questions (serial `for` loop over todo;
   bounded concurrency ~3 could cut a 5-question batch ~2.5x) — needs its own
   measured run; checkpoint/result-append paths look concurrency-safe but are
   unverified under load.
3. Answer-stage prompt size — re-price on a night with sub-second Portal noise.
