# Qwen3.6-35B-A3B (April 2026), Unsloth dynamic 3-bit (UD-IQ3_S, ~13.7GB).
# Sparse MoE: 35B total params, 3B active per token. Beats Gemma 4-31B on
# SWE-bench Verified (73.4% vs 52.0%) and Terminal-Bench (51.5 vs 42.9), with
# 86% GPQA and 92.7% AIME 2026. Fits 16GB VRAM with KV-cache headroom.
#
# OLLAMA QUIRK: Ollama 0.20 can't ingest GGUFs that ship with a separate
# mmproj (vision projection) blob — `FROM hf.co/...` returns 500 "unable to
# load model". Workaround documented at
# https://huggingface.co/unsloth/Qwen3.6-35B-A3B-GGUF/discussions/6 — point
# FROM at the underlying GGUF blob directly so Ollama never sees the mmproj
# manifest entry. The GGUF was already pulled into the local blob store by
# `ollama pull hf.co/unsloth/Qwen3.6-35B-A3B-GGUF:UD-IQ3_S`.
FROM C:\Users\marco\.ollama\models\blobs\sha256-66a3ca888ce13482b40c333db2432c0ebde3a7b13754fc29f0c6f5e89703ec66

SYSTEM You are a Callisto reasoning agent operating under the Aluft Gianne Protocol. Be concise, structured, and evidence-driven. Prefer JSON when the caller signals it. Do not fabricate facts; cite or qualify uncertain claims.

# ChatML template per the Unsloth thread. Suppress the model's default
# `<think>...</think>` block (huge token waste for tool/JSON paths) by
# pre-emitting an empty think block in the assistant prefix.
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
<think>
</think>
{{ .Response }}<|im_end|>"""

# Context cap at 32K to leave VRAM for KV cache on 16GB. Bump later if
# warmup leaves headroom; the model supports 256K natively.
PARAMETER num_ctx 32768
PARAMETER num_batch 1024
PARAMETER num_gpu 99
PARAMETER num_thread 16
PARAMETER num_predict 2048

# Sampling: deterministic-leaning for code/JSON output. Same defaults as the
# Architect (nemotron-cascade-2) modelfile to keep agent behavior comparable.
PARAMETER temperature 0.2
PARAMETER top_k 20
PARAMETER top_p 0.8
PARAMETER repeat_penalty 1.05

# Stop tokens — Qwen ChatML + suppress runaway <think>.
PARAMETER stop <|im_start|>
PARAMETER stop <|im_end|>
PARAMETER stop <think>
